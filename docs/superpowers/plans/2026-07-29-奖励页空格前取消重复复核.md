# 奖励页空格前取消重复复核 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 奖励页已经识别成功后，按 `Space` 前不再重复复核 `craft.reward`。

**Architecture:** 保留 `ProductionCraftTrialDriver::press_space` 现有倒计时、聚焦、输入和鼠标停放，只删除重复 stage 与模板复核。使用源码结构回归测试锁定 production method，避免为两行删除增加仅供测试的 runtime 抽象。

**Tech Stack:** Rust、Tokio、Cargo test、Tauri 2

---

### Task 1: TDD 删除奖励页重复复核

**Files:**
- Modify: `src-tauri/src/special_ops/craft_runtime.rs:329`
- Test: `src-tauri/src/special_ops/craft_runtime.rs`

- [ ] **Step 1: 添加失败测试**

在 `negative_guard_tests` 中添加：

```rust
#[test]
fn production_press_space_does_not_reverify_reward_template() {
    let source = include_str!("craft_runtime.rs");
    let implementation = source
        .split_once("impl CraftTrialDriver for ProductionCraftTrialDriver")
        .unwrap()
        .1;
    let press_space = implementation
        .split_once("    async fn press_space(")
        .unwrap()
        .1
        .split_once("    async fn wait_button(")
        .unwrap()
        .0;

    assert!(!press_space.contains("正在复核 craft.reward"));
    assert!(!press_space.contains("self.verify(\"craft.reward\""));
}
```

- [ ] **Step 2: 运行测试并确认 RED**

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml --lib production_press_space_does_not_reverify_reward_template
```

Expected: FAIL，失败原因为 `press_space` 仍包含“正在复核 craft.reward”或 `self.verify("craft.reward")`。

- [ ] **Step 3: 写最小生产改动**

从 `ProductionCraftTrialDriver::press_space` 删除：

```rust
self.emit_update(LoginRunStatus::Waiting, "正在复核 craft.reward")?;
self.verify("craft.reward", Arc::clone(&cancelled)).await?;
```

保留：

```rust
self.countdown(&cancelled).await?;
ensure_not_cancelled(&cancelled)?;
self.emit_update(LoginRunStatus::Waiting, "正在聚焦游戏窗口")?;
self.focus().await?;
self.emit_update(LoginRunStatus::Inputting, "正在按空格关闭奖励页")?;
```

- [ ] **Step 4: 运行定向测试并确认 GREEN**

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml --lib production_press_space_does_not_reverify_reward_template
cargo test --manifest-path src-tauri/Cargo.toml --lib special_ops::craft_runtime::negative_guard_tests
```

Expected: 两条命令均 exit code 0，0 failed。

### Task 2: 同步文档并验证

**Files:**
- Modify: `droid-wiki/features/special-ops.md`

- [ ] **Step 1: 更新奖励页说明**

明确记录：`open_station` 成功识别奖励页后，`press_space` 信任该结果，不在按键前重复复核 `craft.reward`。

- [ ] **Step 2: 刷新 CodeGraph 并验证**

Run:

```powershell
codegraph sync
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo test --manifest-path src-tauri/Cargo.toml --lib special_ops::tests
cargo test --manifest-path src-tauri/Cargo.toml --lib special_ops::craft_runtime::negative_guard_tests
git diff --check
git status --short --branch
```

Expected: CodeGraph、fmt 和测试命令均 exit code 0；测试 0 failed；`git diff --check` exit code 0。保留既有工作树改动。

- [ ] **Step 3: 等待实机复测**

用户重启开发版后测试可收取制作台。预期奖励页识别成功后，操作窗口不再显示“正在复核 craft.reward”，倒计时结束后直接按 `Space`，随后直接点击当前制作台。实机确认前不提交生产代码。
