# Issues 78 77 76 71 79 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按风险顺序修复 Recognition 自定义点击框选、同键监听、卡片分组/折叠/排序，并用诊断闭环收敛 Profile 切换卡死与同输出按键不触发。

**Architecture:** 先处理确定性小 bug：允许 Recognition 自定义点击区域为空时作为草稿保存，框选提交后再变成可执行效果。再放宽 Recognition 同 scope 监听热键重复限制，复用现有 HotkeyManager 多 registration dispatch 能力。最后把 Recognition 卡片 UI 扩展为带 group/order/collapsed 的持久化模型；#71/#79 不直接猜修，先补可观测性和复现测试。

**Tech Stack:** Tauri 2, Rust, React 19, TypeScript, Vite, Bun, Vitest, Cargo test, daisyUI, `@remixicon/react`.

---

## Scope Check

#78 和 #77 是同一 Recognition 配置/热键域，可在一个短分支完成。#76 涉及持久化结构、UI 布局和 wiki，必须独立提交。#71 和 #79 目前缺少可复现证据，不能和功能改动混在同一提交；先加诊断日志和最小测试。

执行顺序：

1. #78：修自定义点击区域框选前保存失败。
2. #77：允许多个 Recognition 卡片复用同一个监听热键。
3. #76：Recognition 卡片折叠、分组、排序。
4. #79：为同输出按键不触发补复现测试/日志，再按证据修。
5. #71：为 profile apply 增加切换中状态、串行化、日志；再决定是否异步化窗口 reconcile。

阶段提交规则：

- 每完成一个阶段必须立即提交一次，不能把多个阶段攒成一个 commit。
- 一个阶段 = 本计划中的一个 Task，除 #76 拆成两个阶段：数据模型阶段和 UI 阶段。
- 每个阶段提交前必须运行该阶段列出的局部测试；最终阶段再运行全量测试。
- 提交信息使用中文，且能直接看出对应 issue 或模块。
- 若某阶段测试失败，禁止提交“半成品修复”；先修到该阶段局部测试通过，再提交。

## File Structure

- `src/components/app/recognition-utils.ts`
  - 负责 Recognition form 和持久化 settings 互转。
  - #78 修改 `parseClickEffect`：允许 `customRegion: null` 作为草稿态。
- `src/components/app/recognition-utils.test.ts`
  - 覆盖 #78 草稿保存、提交后可执行、旧错误路径变更。
- `src/components/app/recognition-page.tsx`
  - #78 保持 `handleBeginRegionSelection` 调用链，但 `flushSettings` 不再被草稿态阻断。
  - #76 增加分组、排序、折叠 UI。
- `src/components/app/recognition-types.ts`
  - #76 增加 `RecognitionGroup`、`groupId`、`cardGroups`、form 层 `collapsed`。
- `src-tauri/src/recognition/types.rs`
  - #76 增加 Rust 持久化类型字段，保持 `#[serde(rename_all = "camelCase")]`。
- `src-tauri/src/recognition/mod.rs`
  - #77 修改 `validate_hotkey_duplicates`，允许 listener-listener 重复，继续禁止 output step 等于任意 listener。
  - #76 增加 normalize group/order 逻辑。
  - #79 增加触发链路日志和必要测试。
- `src-tauri/src/recognition/effects.rs`
  - #78 确认 `customRegion: null` 不产生 click point；已有逻辑保留，补测试即可。
- `src-tauri/src/profile/mod.rs`
  - #71 增加 profile apply 串行化、日志字段、切换阶段保护。
- `src/hooks/use-profile.tsx`
  - #71 暴露 `switchingProfileId` / `profileSwitching`，ProfileSwitcher 禁用重复切换。
- `src/components/app/profile-switcher.tsx`
  - #71 切换期间禁用菜单项并显示明确状态。
- `droid-wiki/features/recognition.md`
  - #78/#77/#76 同步行为、字段、热键规则。
- `droid-wiki/systems/profile-system.md`
  - #71 同步 profile apply 串行化和诊断策略。

---

### Task 1: #78 自定义点击区域草稿态可保存

**Files:**
- Modify: `src/components/app/recognition-utils.ts`
- Modify: `src/components/app/recognition-utils.test.ts`
- Modify: `src-tauri/src/recognition/mod.rs`
- Modify: `droid-wiki/features/recognition.md`

- [x] **Step 1: 写前端失败测试，证明 customRegion=null 可作为框选前草稿保存**

在 `src/components/app/recognition-utils.test.ts` 的 `parseSettingsForm` 相关 describe 内新增：

```ts
it("允许自定义点击区域尚未框选时保存为草稿态", () => {
    const form = makeValidForm();
    form.cards[0] = {
        ...form.cards[0],
        triggerMode: "hotkey",
        hotkey: "F8",
        audioEffectEnabled: false,
        hotkeyEffectEnabled: false,
        clickEffectEnabled: true,
        clickMode: "customRegion",
        clickCustomRegion: null,
    };

    const settings = parseSettingsForm(form);

    expect(settings.cards[0].effects?.click).toEqual({
        mode: "customRegion",
        customRegion: null,
        colorProbeIndex: null,
    });
});
```

- [x] **Step 2: 运行前端单测，确认失败**

Run:

```bash
bunx vitest run src/components/app/recognition-utils.test.ts
```

Expected: FAIL，错误来自 `parseClickEffect` 抛出“必须框选自定义点击区域”。

- [x] **Step 3: 修改 `parseClickEffect`，允许草稿态落盘**

把 `src/components/app/recognition-utils.ts` 中 `parseClickEffect` 的 `customRegion` 分支替换为：

```ts
function parseClickEffect(form: RecognitionCardForm, colorProbeCount: number): RecognitionClickEffect {
    const clickMode = form.clickMode ?? "customRegion";
    if (clickMode === "customRegion") {
        return {
            mode: "customRegion",
            customRegion: form.clickCustomRegion ?? null,
            colorProbeIndex: null,
        };
    }
    if (form.triggerMode === "hotkey") {
        throw new Error("快捷键触发的点击效果必须使用自定义区域。");
    }
    if (form.triggerMode === "colorWatch") {
        const index = parseInt(form.clickColorProbeIndex ?? "", 10);
        if (Number.isNaN(index) || index < 0 || index >= colorProbeCount) {
            throw new Error("识色点击效果必须选择有效探针。");
        }
        return {
            mode: "recognitionRegion",
            customRegion: null,
            colorProbeIndex: index,
        };
    }
    return {
        mode: "recognitionRegion",
        customRegion: null,
        colorProbeIndex: null,
    };
}
```

- [x] **Step 4: 写 Rust 失败测试，证明后端也允许 customRegion=None 草稿保存**

在 `src-tauri/src/recognition/mod.rs` 的 tests 中，把现有 `validate_rejects_missing_custom_click_region` 改为：

```rust
#[test]
fn validate_accepts_missing_custom_click_region_as_draft() {
    let mut card = base_card();
    card.effects.audio = None;
    card.effects.click = Some(types::RecognitionClickEffect {
        mode: types::RecognitionClickMode::CustomRegion,
        custom_region: None,
        color_probe_index: None,
    });

    let settings = RecognitionSettings {
        recognition_enabled: true,
        cards: vec![card],
    };

    validate_settings(&settings).unwrap();
}
```

- [x] **Step 5: 运行 Rust 测试，确认失败**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml recognition::tests::validate_accepts_missing_custom_click_region_as_draft
```

Expected: FAIL，错误包含“点击效果必须框选自定义区域”。

- [x] **Step 6: 修改后端校验，允许草稿态但保持运行期不可执行**

在 `src-tauri/src/recognition/mod.rs` 的 `validate_settings` 中，把 `CustomRegion` 分支改为不报错：

```rust
if let Some(effect) = &card.effects.click {
    match effect.mode {
        types::RecognitionClickMode::CustomRegion => {
            executable_effect_count += 1;
        }
        types::RecognitionClickMode::RecognitionRegion => {
            if card.trigger_mode == RecognitionTriggerMode::Hotkey {
                return Err(format!(
                    "卡片 {} 的快捷键触发点击必须使用自定义区域",
                    card.name
                ));
            }
            if card.trigger_mode == RecognitionTriggerMode::ColorWatch {
                let Some(index) = effect.color_probe_index else {
                    return Err(format!("卡片 {} 的识色点击效果必须选择探针", card.name));
                };
                if index >= card.color_probes.len() {
                    return Err(format!("卡片 {} 的识色点击探针不存在", card.name));
                }
            }
            executable_effect_count += 1;
        }
    }
}
```

保留 `src-tauri/src/recognition/effects.rs` 中 `click_point_for_effect` 现有行为：`custom_region: None` 返回 `None`。

- [x] **Step 7: 运行 #78 相关测试**

Run:

```bash
bunx vitest run src/components/app/recognition-utils.test.ts
cargo test --manifest-path src-tauri/Cargo.toml recognition::tests::validate_accepts_missing_custom_click_region_as_draft recognition::effects::tests::missing_custom_click_region_has_no_click_point
```

Expected: PASS。

- [x] **Step 8: 更新 wiki**

在 `droid-wiki/features/recognition.md` 的点击效果说明附近加入：

```md
- 自定义点击区域支持草稿态：启用点击效果但尚未框选 `customRegion` 时，配置可保存，便于打开框选 overlay；实际触发时没有可点击坐标，不执行点击。框选提交后写入 `customRegion`，效果变为可执行。
```

- [x] **Step 9: 提交 #78**

```bash
git add src/components/app/recognition-utils.ts src/components/app/recognition-utils.test.ts src-tauri/src/recognition/mod.rs droid-wiki/features/recognition.md
git commit -m "fix(recognition): 允许自定义点击区域草稿保存"
```

---

### Task 2: #77 允许多个 Recognition 卡片使用同一监听热键

**Files:**
- Modify: `src-tauri/src/recognition/mod.rs`
- Modify: `src-tauri/src/hotkeys.rs`
- Modify: `droid-wiki/features/recognition.md`

- [x] **Step 1: 写失败测试，明确同一触发热键应合法**

在 `src-tauri/src/recognition/mod.rs` tests 中，把 `validate_rejects_duplicate_listener_hotkeys_across_cards` 改为：

```rust
#[test]
fn validate_accepts_duplicate_listener_hotkeys_across_cards() {
    let mut card_a = base_card();
    card_a.id = "c1".into();
    card_a.name = "卡片 A".into();
    card_a.hotkey = Some("Ctrl+F1".into());

    let mut card_b = base_card();
    card_b.id = "c2".into();
    card_b.name = "卡片 B".into();
    card_b.hotkey = Some("Ctrl+F1".into());

    let settings = RecognitionSettings {
        recognition_enabled: true,
        cards: vec![card_a, card_b],
    };

    validate_settings(&settings).unwrap();
}
```

- [x] **Step 2: 运行测试，确认失败**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml recognition::tests::validate_accepts_duplicate_listener_hotkeys_across_cards
```

Expected: FAIL，错误包含“重复”。

- [x] **Step 3: 修改 duplicate validation，只禁止 output step 等于 listener**

把 `src-tauri/src/recognition/mod.rs` 的 `validate_hotkey_duplicates` 替换为：

```rust
fn validate_hotkey_duplicates(settings: &RecognitionSettings) -> Result<(), String> {
    let mut listener_set = std::collections::HashSet::<String>::new();
    for card in settings.cards.iter().filter(|c| c.enabled) {
        if card.trigger_mode == RecognitionTriggerMode::Hotkey {
            if let Some(key) = card.hotkey.as_ref().filter(|v| !v.trim().is_empty()) {
                listener_set.insert(crate::hotkey_types::hotkey_to_string(key)?);
            }
        } else if card.activation.mode != types::RecognitionActivationMode::Always {
            if let Some(key) = card
                .activation
                .hotkey
                .as_ref()
                .filter(|v| !v.trim().is_empty())
            {
                listener_set.insert(crate::hotkey_types::hotkey_to_string(key)?);
            }
        }
    }

    for card in settings.cards.iter().filter(|c| c.enabled) {
        if let Some(effect) = card.effects.hotkey.as_ref() {
            for step in effect.normalized_steps() {
                if step.hotkey.trim().is_empty() {
                    continue;
                }
                let normalized = crate::hotkey_types::hotkey_to_string(&step.hotkey)?;
                if listener_set.contains(&normalized) {
                    return Err(format!(
                        "卡片 {} 的按键效果不能和监听热键相同：{normalized}",
                        card.name
                    ));
                }
            }
        }
    }
    Ok(())
}
```

- [x] **Step 4: 写 HotkeyManager 多 action dispatch 测试，防止后续回归**

在 `src-tauri/src/hotkeys.rs` tests 中新增纯函数测试：

```rust
#[test]
fn actions_for_key_state_returns_all_same_scope_duplicate_bindings() {
    let calls = Arc::new(Mutex::new(Vec::<&'static str>::new()));
    let calls_a = Arc::clone(&calls);
    let action_a: HotkeyAction = Arc::new(move |_app| {
        calls_a.lock().unwrap().push("a");
    });
    let calls_b = Arc::clone(&calls);
    let action_b: HotkeyAction = Arc::new(move |_app| {
        calls_b.lock().unwrap().push("b");
    });

    let registrations = Arc::new(Mutex::new(vec![
        HotkeyRegistration {
            scope: "recognition".into(),
            binding: HotkeyBinding::parse("F8").unwrap(),
            enabled: true,
            display_name: "识别触发".into(),
            conflict_policy: ConflictPolicy::AllowHold,
            action: action_a,
        },
        HotkeyRegistration {
            scope: "recognition".into(),
            binding: HotkeyBinding::parse("F8").unwrap(),
            enabled: true,
            display_name: "识别触发".into(),
            conflict_policy: ConflictPolicy::AllowHold,
            action: action_b,
        },
    ]));
    let key_state = KeyState {
        primary: PrimaryKey::Function(8),
        modifiers: HashSet::new(),
    };

    let actions = actions_for_key_state(&registrations, &key_state);

    assert_eq!(actions.len(), 2);
}
```

- [x] **Step 5: 运行 #77 测试**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml recognition::tests::validate_accepts_duplicate_listener_hotkeys_across_cards hotkeys::tests::actions_for_key_state_returns_all_same_scope_duplicate_bindings
```

Expected: PASS。

- [x] **Step 6: 更新 wiki 热键规则**

在 `droid-wiki/features/recognition.md` 的热键规则段落写入：

```md
- Recognition 同一 scope 内允许多个启用卡片复用同一个监听热键；命中后会触发所有匹配卡片。
- 输出按键 step 仍不得等于任意监听热键，避免按键效果递归触发 Recognition。
- 输出按键 step 之间允许重复，同卡片和跨卡片均允许。
```

- [x] **Step 7: 提交 #77**

```bash
git add src-tauri/src/recognition/mod.rs src-tauri/src/hotkeys.rs droid-wiki/features/recognition.md
git commit -m "fix(recognition): 允许多卡片复用监听热键"
```

---

### Task 3: #76 Recognition 卡片分组、折叠、排序数据模型

**Files:**
- Modify: `src/components/app/recognition-types.ts`
- Modify: `src/components/app/recognition-utils.ts`
- Modify: `src/components/app/recognition-utils.test.ts`
- Modify: `src-tauri/src/recognition/types.rs`
- Modify: `src-tauri/src/recognition/mod.rs`

- [ ] **Step 1: 前端类型增加 group model**

在 `src/components/app/recognition-types.ts` 新增：

```ts
export type RecognitionGroup = {
    id: string;
    name: string;
    order: number;
    collapsed: boolean;
};
```

修改 `RecognitionCard`：

```ts
export type RecognitionCard = {
    id: string;
    groupId?: string | null;
    order?: number | null;
    name: string;
    enabled: boolean;
    triggerMode: RecognitionTriggerMode;
    hotkey: string | null;
    watchRegion: RegionRect | null;
    watchReferenceImagePath: string | null;
    watchMatchThreshold: number;
    watchPollIntervalMs: number;
    activation?: RecognitionActivation | null;
    effects?: RecognitionEffects | null;
    cooldownMs: number;
    colorProbes: ColorProbe[];
    colorMatchMode: ColorMatchMode;
    colorMatchMethod: ColorMatchMethod;
    audioFiles?: string[];
    playMode?: RecognitionPlayMode;
    comboWindowMs?: number;
    comboWindows?: number[];
    volume?: number;
    allowSimultaneous?: boolean;
};
```

修改 `RecognitionSettings` 和 `RecognitionSettingsForm`：

```ts
export type RecognitionSettings = {
    recognitionEnabled?: boolean;
    audioEnabled?: boolean;
    cardGroups?: RecognitionGroup[];
    cards: RecognitionCard[];
};

export type RecognitionSettingsForm = {
    recognitionEnabled?: boolean;
    audioEnabled: boolean;
    cardGroups: RecognitionGroup[];
    cards: RecognitionCardForm[];
};
```

修改 `RecognitionCardForm` 增加：

```ts
groupId: string;
order: number;
collapsed?: boolean;
```

- [ ] **Step 2: 写前端 normalize 失败测试**

在 `src/components/app/recognition-utils.test.ts` 新增：

```ts
it("settingsToForm 为旧配置补默认分组并按 order 排序", () => {
    const settings = makeValidSettings();
    settings.cardGroups = undefined;
    settings.cards = [
        {...settings.cards[0], id: "b", name: "B", order: 20, groupId: null},
        {...settings.cards[0], id: "a", name: "A", order: 10, groupId: null},
    ];

    const form = settingsToForm(settings);

    expect(form.cardGroups).toEqual([
        {id: "default-recognition-group", name: "默认分组", order: 0, collapsed: false},
    ]);
    expect(form.cards.map((card) => card.id)).toEqual(["a", "b"]);
    expect(form.cards.every((card) => card.groupId === "default-recognition-group")).toBe(true);
});
```

- [ ] **Step 3: 实现前端 form normalization**

在 `src/components/app/recognition-utils.ts` 增加常量和 helper：

```ts
export const DEFAULT_RECOGNITION_GROUP_ID = "default-recognition-group";

function defaultRecognitionGroup(): RecognitionGroup {
    return {
        id: DEFAULT_RECOGNITION_GROUP_ID,
        name: "默认分组",
        order: 0,
        collapsed: false,
    };
}

function normalizeRecognitionGroups(settings: RecognitionSettings): RecognitionGroup[] {
    const groups = (settings.cardGroups ?? [])
        .map((group) => ({
            id: group.id.trim() || DEFAULT_RECOGNITION_GROUP_ID,
            name: group.name.trim() || "未命名分组",
            order: Number.isFinite(group.order) ? group.order : 0,
            collapsed: group.collapsed ?? false,
        }))
        .filter((group, index, all) => all.findIndex((item) => item.id === group.id) === index);
    if (!groups.some((group) => group.id === DEFAULT_RECOGNITION_GROUP_ID)) {
        groups.unshift(defaultRecognitionGroup());
    }
    return groups.sort((a, b) => a.order - b.order);
}
```

在 `settingsToForm` 中使用：

```ts
export function settingsToForm(settings: RecognitionSettings): RecognitionSettingsForm {
    const recognitionEnabled = settings.recognitionEnabled ?? settings.audioEnabled ?? true;
    const cardGroups = normalizeRecognitionGroups(settings);
    const groupIds = new Set(cardGroups.map((group) => group.id));
    return {
        recognitionEnabled,
        audioEnabled: recognitionEnabled,
        cardGroups,
        cards: settings.cards
            .map((card, index) => cardToForm({
                ...card,
                groupId: card.groupId && groupIds.has(card.groupId) ? card.groupId : DEFAULT_RECOGNITION_GROUP_ID,
                order: Number.isFinite(card.order ?? NaN) ? card.order : index,
            }))
            .sort((a, b) => a.order - b.order),
    };
}
```

- [ ] **Step 4: Rust 类型增加 group model**

在 `src-tauri/src/recognition/types.rs` 新增：

```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub struct RecognitionGroup {
    pub id: String,
    pub name: String,
    #[serde(default)]
    pub order: i32,
    #[serde(default)]
    pub collapsed: bool,
}
```

修改 `RecognitionCard` 增加：

```rust
#[serde(default)]
pub group_id: Option<String>,
#[serde(default)]
pub order: i32,
```

修改 `RecognitionSettings` 增加：

```rust
#[serde(default)]
pub card_groups: Vec<RecognitionGroup>,
```

- [ ] **Step 5: Rust normalize 增加分组归一化**

在 `src-tauri/src/recognition/mod.rs` 增加：

```rust
const DEFAULT_RECOGNITION_GROUP_ID: &str = "default-recognition-group";

fn default_recognition_group() -> types::RecognitionGroup {
    types::RecognitionGroup {
        id: DEFAULT_RECOGNITION_GROUP_ID.to_string(),
        name: "默认分组".to_string(),
        order: 0,
        collapsed: false,
    }
}
```

在 `normalize_settings` 中归一化：

```rust
pub(crate) fn normalize_settings(mut settings: RecognitionSettings) -> RecognitionSettings {
    let mut groups = settings
        .card_groups
        .into_iter()
        .map(|mut group| {
            group.id = group.id.trim().to_string();
            if group.id.is_empty() {
                group.id = DEFAULT_RECOGNITION_GROUP_ID.to_string();
            }
            group.name = group.name.trim().to_string();
            if group.name.is_empty() {
                group.name = "未命名分组".to_string();
            }
            group
        })
        .collect::<Vec<_>>();
    if !groups.iter().any(|group| group.id == DEFAULT_RECOGNITION_GROUP_ID) {
        groups.push(default_recognition_group());
    }
    groups.sort_by_key(|group| group.order);
    groups.dedup_by(|a, b| a.id == b.id);
    let group_ids = groups.iter().map(|group| group.id.clone()).collect::<std::collections::HashSet<_>>();
    for (index, card) in settings.cards.iter_mut().enumerate() {
        let group_id = card.group_id.clone().unwrap_or_default();
        if group_id.trim().is_empty() || !group_ids.contains(group_id.trim()) {
            card.group_id = Some(DEFAULT_RECOGNITION_GROUP_ID.to_string());
        }
        if card.order == 0 {
            card.order = index as i32;
        }
    }
    settings.card_groups = groups;
    settings.cards.sort_by_key(|card| card.order);
    settings
}
```

- [ ] **Step 6: 运行类型/normalize 测试**

Run:

```bash
bunx vitest run src/components/app/recognition-utils.test.ts
cargo test --manifest-path src-tauri/Cargo.toml recognition::types recognition::tests
```

Expected: PASS。

- [ ] **Step 7: 提交 #76 数据模型**

```bash
git add src/components/app/recognition-types.ts src/components/app/recognition-utils.ts src/components/app/recognition-utils.test.ts src-tauri/src/recognition/types.rs src-tauri/src/recognition/mod.rs
git commit -m "feat(recognition): 增加识别卡片分组模型"
```

---

### Task 4: #76 Recognition 卡片折叠、分组、排序 UI

**Files:**
- Modify: `src/components/app/recognition-page.tsx`
- Modify: `src/components/app/recognition-page.test.ts`
- Modify: `droid-wiki/features/recognition.md`

- [ ] **Step 1: 写 UI 行为测试**

在 `src/components/app/recognition-page.test.ts` 增加：

```ts
it("分组折叠时隐藏组内卡片详情但保留标题行", async () => {
    renderRecognitionPageWithBootstrap({
        settings: {
            recognitionEnabled: true,
            cardGroups: [{id: "g1", name: "战斗", order: 0, collapsed: true}],
            cards: [makeRecognitionCard({id: "c1", groupId: "g1", name: "卡片一"})],
        },
        hotkeyError: null,
    });

    expect(await screen.findByText("战斗")).toBeInTheDocument();
    expect(screen.getByText("卡片一")).toBeInTheDocument();
    expect(screen.queryByLabelText("匹配阈值")).not.toBeInTheDocument();
});
```

- [ ] **Step 2: 实现分组渲染 helper**

在 `src/components/app/recognition-page.tsx` 增加：

```ts
function cardsForGroup(form: RecognitionSettingsForm, groupId: string) {
    return form.cards
        .map((card, index) => ({card, index}))
        .filter((item) => item.card.groupId === groupId)
        .sort((a, b) => a.card.order - b.card.order);
}
```

- [ ] **Step 3: 加折叠切换**

在页面组件内增加：

```ts
const updateGroupById = useCallback((groupId: string, patch: Partial<RecognitionGroup>) => {
    setForm((current) => {
        if (!current) return current;
        return {
            ...current,
            cardGroups: current.cardGroups.map((group) =>
                group.id === groupId ? {...group, ...patch} : group,
            ),
        };
    });
}, [setForm]);
```

分组标题按钮使用 `@remixicon/react` 图标：

```tsx
<Button
    variant="ghost"
    size="sm"
    onClick={() => updateGroupById(group.id, {collapsed: !group.collapsed})}
    data-icon="inline-start"
>
    {group.collapsed ? <RiArrowRightSLine className="size-4" aria-hidden="true"/> : <RiArrowDownSLine className="size-4" aria-hidden="true"/>}
    {group.name}
</Button>
```

- [ ] **Step 4: 加组内上下排序**

给每张卡片标题区增加两个按钮：

```tsx
<Button
    variant="ghost"
    size="sm"
    disabled={position === 0}
    onClick={() => moveCardWithinGroup(group.id, card.id, -1)}
    aria-label="上移卡片"
>
    <RiArrowUpSLine className="size-4" aria-hidden="true"/>
</Button>
<Button
    variant="ghost"
    size="sm"
    disabled={position === groupCards.length - 1}
    onClick={() => moveCardWithinGroup(group.id, card.id, 1)}
    aria-label="下移卡片"
>
    <RiArrowDownSLine className="size-4" aria-hidden="true"/>
</Button>
```

实现 reorder：

```ts
const moveCardWithinGroup = useCallback((groupId: string, cardId: string, delta: -1 | 1) => {
    setForm((current) => {
        if (!current) return current;
        const groupCards = current.cards
            .filter((card) => card.groupId === groupId)
            .sort((a, b) => a.order - b.order);
        const index = groupCards.findIndex((card) => card.id === cardId);
        const nextIndex = index + delta;
        if (index < 0 || nextIndex < 0 || nextIndex >= groupCards.length) return current;
        const reordered = [...groupCards];
        [reordered[index], reordered[nextIndex]] = [reordered[nextIndex], reordered[index]];
        const orderById = new Map(reordered.map((card, order) => [card.id, order]));
        return {
            ...current,
            cards: current.cards.map((card) =>
                card.groupId === groupId ? {...card, order: orderById.get(card.id) ?? card.order} : card,
            ),
        };
    });
}, [setForm]);
```

- [ ] **Step 5: 加分组增删改名**

新增按钮 handler：

```ts
const addRecognitionGroup = useCallback(() => {
    setForm((current) => {
        if (!current) return current;
        const id = `recognition-group-${Date.now().toString(36)}`;
        return {
            ...current,
            cardGroups: [
                ...current.cardGroups,
                {id, name: "新分组", order: current.cardGroups.length, collapsed: false},
            ],
        };
    });
}, [setForm]);
```

删除空分组：

```ts
const removeEmptyRecognitionGroup = useCallback((groupId: string) => {
    setForm((current) => {
        if (!current) return current;
        if (groupId === DEFAULT_RECOGNITION_GROUP_ID) return current;
        if (current.cards.some((card) => card.groupId === groupId)) return current;
        return {
            ...current,
            cardGroups: current.cardGroups.filter((group) => group.id !== groupId),
        };
    });
}, [setForm]);
```

- [ ] **Step 6: 更新 wiki**

在 `droid-wiki/features/recognition.md` 增加：

```md
- 卡片支持分组、组内排序和折叠。分组持久化字段为 `cardGroups`，卡片持久化字段为 `groupId` 和 `order`。
- 旧配置缺少分组字段时自动归入 `default-recognition-group`。
```

- [ ] **Step 7: 运行 #76 UI 验证**

Run:

```bash
bunx vitest run src/components/app/recognition-page.test.ts src/components/app/recognition-utils.test.ts
bun run build
```

Expected: PASS / build 通过。

- [ ] **Step 8: 提交 #76 UI**

```bash
git add src/components/app/recognition-page.tsx src/components/app/recognition-page.test.ts droid-wiki/features/recognition.md
git commit -m "feat(recognition): 支持卡片分组折叠排序"
```

---

### Task 5: #79 同输出按键不触发的证据链

**Files:**
- Modify: `src-tauri/src/recognition/effects.rs`
- Modify: `src-tauri/src/recognition/mod.rs`
- Modify: `droid-wiki/features/recognition.md`

- [ ] **Step 1: 保留已存在的重复输出校验测试**

确认以下测试存在并通过：

```bash
cargo test --manifest-path src-tauri/Cargo.toml recognition::tests::validate_accepts_repeated_output_steps_across_cards
```

Expected: PASS。

- [ ] **Step 2: 给 effects 执行链加结构化日志**

在 `src-tauri/src/recognition/effects.rs` 的 `execute` 中，`let plan = build_plan...` 后加入：

```rust
crate::log_info!(
    "recognition",
    "准备执行触发效果",
    "card_id" => card_id.clone(),
    "hotkey_step_count" => plan.hotkey_steps.len(),
    "has_click" => plan.click_point.is_some(),
    "has_audio" => plan.audio.is_some()
);
```

在 hotkey step loop 内加入：

```rust
crate::log_info!(
    "recognition",
    "执行按键效果 step",
    "card_id" => card_id.clone(),
    "hotkey" => step.hotkey.clone(),
    "delay_ms" => step.delay_ms
);
```

- [ ] **Step 3: 给热键回调注册链加日志**

在 `src-tauri/src/recognition/mod.rs` 的 `restart_hotkey_listeners` 中，`bindings.push((key.clone(), action));` 前加入：

```rust
crate::log_info!(
    "recognition",
    "注册识别监听热键",
    "card_id" => card.id.clone(),
    "card_name" => card.name.clone(),
    "hotkey" => key.clone(),
    "trigger_mode" => format!("{:?}", card.trigger_mode)
);
```

- [ ] **Step 4: 手工复现 #79 并抓日志**

Run:

```bash
bun run tauri dev
```

手工配置：

```text
卡片 1：触发热键 A，按键效果 D，triggerCount=1
卡片 2：触发热键 B，按键效果 D，triggerCount=1
卡片 3：触发热键 C，按键效果 D，triggerCount=1
```

Expected:

```text
按 A/B/C 后日志各出现一次：
准备执行触发效果 card_id=<对应卡片>
执行按键效果 step hotkey=D
```

如果日志出现但游戏内无 D：问题在 `input_simulation::press_hotkey_once` / Enigo / 前台窗口焦点。  
如果日志不出现：问题在 HotkeyManager dispatch 或 Recognition listener 注册。  
如果日志只出现 A：检查 #77 是否已放宽同监听热键，检查卡片 enabled 和全局开关。

- [ ] **Step 5: 提交 #79 诊断日志**

```bash
git add src-tauri/src/recognition/effects.rs src-tauri/src/recognition/mod.rs droid-wiki/features/recognition.md
git commit -m "chore(recognition): 增加同输出按键触发诊断日志"
```

---

### Task 6: #71 Profile 切换串行化与切换中 UI

**Files:**
- Modify: `src-tauri/src/profile/mod.rs`
- Modify: `src/hooks/use-profile.tsx`
- Modify: `src/components/app/profile-switcher.tsx`
- Modify: `src/components/app/profile-switcher.test.ts`
- Modify: `droid-wiki/systems/profile-system.md`

- [ ] **Step 1: 后端 ProfileState 增加 apply lock**

把 `src-tauri/src/profile/mod.rs` 中 `ProfileState` 改为：

```rust
pub struct ProfileState {
    settings: Mutex<ProfileSettings>,
    apply_lock: Mutex<()>,
}

impl ProfileState {
    fn new(settings: ProfileSettings) -> Self {
        Self {
            settings: Mutex::new(settings),
            apply_lock: Mutex::new(()),
        }
    }
}
```

- [ ] **Step 2: `profile_apply` 串行化并加耗时日志**

修改 `profile_apply`：

```rust
pub fn profile_apply(
    app: AppHandle,
    state: State<'_, ProfileState>,
    id: String,
) -> Result<(), String> {
    let started_at = std::time::Instant::now();
    let _apply_guard = state
        .apply_lock
        .lock()
        .map_err(|_| "Profile 切换锁已损坏".to_string())?;
    crate::log_info!("profile", "开始切换 Profile", "profile_id" => id.clone());

    let snapshot = {
        let settings = state.settings.lock().map_err(|_| "Profile 状态锁已损坏")?;
        let profile = settings
            .profiles
            .iter()
            .find(|p| p.id == id)
            .ok_or_else(|| format!("找不到配置 {id}"))?;
        profile.snapshot.clone()
    };

    apply_snapshot_to_tools(&app, &snapshot)?;

    {
        let mut settings = state.settings.lock().map_err(|_| "Profile 状态锁已损坏")?;
        settings.active_profile_id = id.clone();
        settings::save_settings(&app, &settings)?;
    }

    emit_profile_changed(&app, &build_bootstrap(&state));
    crate::log_info!(
        "profile",
        "完成切换 Profile",
        "profile_id" => id,
        "elapsed_ms" => started_at.elapsed().as_millis().to_string()
    );
    Ok(())
}
```

- [ ] **Step 3: 前端 context 暴露切换中状态**

在 `src/hooks/use-profile.tsx` 的 context type 增加：

```ts
switchingProfileId: string | null;
```

Provider 内增加：

```ts
const [switchingProfileId, setSwitchingProfileId] = useState<string | null>(null);
```

修改 `switchProfile`：

```ts
const switchProfile = useCallback(
    async (id: string) => {
        if (switchingProfileId) return;
        setSwitchingProfileId(id);
        try {
            await invoke("profile_apply", {id});
            await refreshAfterSwitch();
        } catch (err: unknown) {
            setError(String(err));
            throw err;
        } finally {
            setSwitchingProfileId(null);
        }
    },
    [refreshAfterSwitch, switchingProfileId],
);
```

value 中加入 `switchingProfileId`。

- [ ] **Step 4: ProfileSwitcher 禁用重复点击**

在 `src/components/app/profile-switcher.tsx` 读取：

```ts
const {bootstrap, switchProfile, switchingProfileId} = useProfile();
```

菜单项禁用条件：

```tsx
disabled={Boolean(switchingProfileId) || profile.id === bootstrap?.activeProfileId}
```

切换中文案：

```tsx
{switchingProfileId === profile.id ? "切换中" : profile.name}
```

- [ ] **Step 5: 写前端测试**

在 `src/components/app/profile-switcher.test.ts` 新增：

```ts
it("Profile 切换中禁用其他 Profile 点击", async () => {
    mockUseProfile.mockReturnValue({
        ...baseProfileContext,
        switchingProfileId: "p2",
    });

    render(<ProfileSwitcher />);

    await userEvent.click(screen.getByRole("button", {name: /配置/}));
    expect(screen.getByRole("menuitem", {name: /切换中/})).toHaveAttribute("aria-disabled", "true");
});
```

- [ ] **Step 6: 运行 #71 测试**

Run:

```bash
bunx vitest run src/components/app/profile-switcher.test.ts src/hooks/use-profile.test.ts
cargo test --manifest-path src-tauri/Cargo.toml profile::
```

Expected: PASS。

- [ ] **Step 7: 更新 profile wiki**

在 `droid-wiki/systems/profile-system.md` 增加：

```md
- `profile_apply` 串行执行：后端使用 `apply_lock` 防止并发切换互相覆盖；前端切换期间禁用 ProfileSwitcher，避免启动装载期重复触发。
- 切换过程写入 `profile` 日志，包含 profile_id 和 elapsed_ms，用于排查卡死或窗口创建阻塞。
```

- [ ] **Step 8: 提交 #71 串行化**

```bash
git add src-tauri/src/profile/mod.rs src/hooks/use-profile.tsx src/components/app/profile-switcher.tsx src/components/app/profile-switcher.test.ts droid-wiki/systems/profile-system.md
git commit -m "fix(profile): 串行化配置切换并禁用重复操作"
```

---

### Task 7: 全量验证与发布前检查

**Files:**
- Verify only

- [ ] **Step 1: 运行前端单元测试**

```bash
bun run test
```

Expected: 全部通过。

- [ ] **Step 2: 运行前端构建**

```bash
bun run build
```

Expected: `tsc && vite build` 通过。

- [ ] **Step 3: 运行 Rust 测试**

```bash
cargo test --manifest-path src-tauri/Cargo.toml
```

Expected: 全部通过，doc-test 旧 ignored 状态保持。

- [ ] **Step 4: 手工验证 #78/#77/#79**

```text
#78：启用点击效果 -> 自定义区域中心 -> 点击框选区域 -> overlay 打开 -> Enter 保存 -> 主页面显示区域坐标。
#77：两个 Hotkey 卡片使用同一个触发热键，按一次该键两个卡片都触发。
#79：多个卡片触发不同监听热键但输出同一个 D，A/B/C 各自触发时日志都出现对应 card_id 和 hotkey=D。
```

- [ ] **Step 5: 手工验证 #71**

```text
启动应用后立即打开 ProfileSwitcher，连续点击两个不同 Profile。
预期：第二次点击被 UI 禁用；日志出现开始/完成切换，应用无假死。
```

- [ ] **Step 6: 最终提交检查**

```bash
git status --short
git log --oneline --max-count=8
```

Expected: worktree 干净；提交顺序为 #78 -> #77 -> #76 model -> #76 UI -> #79 diagnostics -> #71 profile。

---

## Self-Review

- Spec coverage: #78/#77/#76/#71/#79 均有任务；#79 明确为诊断优先，避免无证据修。
- Placeholder scan: 本计划不含 TBD、TODO、implement later、fill in details。
- Type consistency: `RecognitionGroup`、`groupId`、`order`、`collapsed` 在 TS/Rust/form/wiki 中命名一致；Rust 对外字段依赖 serde camelCase。
- Test flow: 每个行为改动先有失败测试，再实现，再局部测试，最后全量验证。
