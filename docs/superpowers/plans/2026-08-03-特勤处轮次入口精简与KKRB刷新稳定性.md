# 特勤处轮次入口精简与 KKRB 刷新稳定性 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 删除无效手动轮次入口，稳定 KKRB 名称刷新，并将三段长配置默认折叠。

**Architecture:** UI 仅删入口并使用原生折叠元素；KKRB adapter 公开可判定的 `-101` 分类，command 层控制固定次数重试。后台 `profit` query 路径不接入新 retry helper。

**Tech Stack:** React 19、Vitest、Tauri 2、Rust、reqwest、daisyUI 5。

---

### Task 1: 锁定 UI 行为

**Files:**
- Modify: `src/components/app/special-ops-page.test.tsx`
- Modify: `src/components/app/special-ops-profit-filter.test.tsx`（若不存在则创建）

- [x] **Step 1: 写失败测试**

断言特勤处页面不再出现“开始当前到期轮次”，默认业务配置的“子弹兑换顺序”和账号“独立设置”均位于关闭的 `<details>`；利润页“业务目标”也位于关闭的 `<details>`。

- [x] **Step 2: 运行测试确认失败**

Run: `bunx vitest run src/components/app/special-ops-page.test.tsx src/components/app/special-ops-profit-filter.test.tsx`

Expected: 新断言失败，因为当前按钮与表格均直接 render。

- [x] **Step 3: 最小 UI 实现**

删除仅用于启动 `special_ops_start_due_round` 的 state/handler/button/text；将三段内容包进 `<details className="collapse collapse-arrow">`，使用 `collapse-title` 和 `collapse-content`，不新增状态。

- [x] **Step 4: 运行测试确认通过**

Run: `bunx vitest run src/components/app/special-ops-page.test.tsx src/components/app/special-ops-profit-filter.test.tsx`

Expected: PASS。

### Task 2: 锁定 KKRB `-101` 重试

**Files:**
- Modify: `src-tauri/src/special_ops/profit/kkrb.rs`
- Modify: `src-tauri/src/special_ops/mod.rs`

- [x] **Step 1: 写失败测试**

为 `KkrbSourceError` 写分类测试：`code -101` 可识别；另一个 code 不可识别。为 catalog retry helper 写 async 测试：前两次 `-101`、第三次成功时返回 catalog 且调用 3 次；非 `-101` 时仅调用 1 次；三次 `-101` 返回固定中文错误。

- [x] **Step 2: 运行测试确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml special_ops::profit::kkrb::tests`

Expected: FAIL，因为尚无分类方法和 retry helper。

- [x] **Step 3: 最小后端实现**

在 `KkrbSourceError` 保存可选业务码或新增只读判定方法；`special_ops_fetch_profit_catalog` 使用一个仅此 command 调用的 helper，在 `-101` 后 `tokio::time::sleep(Duration::from_secs(1))`，最多三次。最终错误替换为固定文案；成功结果直接返回。

- [x] **Step 4: 运行测试确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml special_ops::profit::kkrb::tests`

Expected: PASS。

### Task 3: 集成验证与文档同步

**Files:**
- Modify: `droid-wiki/features/special-ops.md`

- [x] **Step 1: 同步行为文档**

记录手动 KKRB 刷新的 `-101` 三次重试与三个默认折叠区；不描述为后台利润查询规则。

- [x] **Step 2: 运行质量检查**

Run: `bun run test`

Expected: PASS。

Run: `cargo check --manifest-path src-tauri/Cargo.toml`

Expected: PASS。

- [x] **Step 3: 刷新 CodeGraph**

Run: `codegraph sync`

Expected: 索引完成，无同步错误。
