# 特勤处人工恢复、利润截止与导航重试实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. 本任务只允许 Inline Execution，不使用 subagent，不创建 worktree，不暂存、不提交。

**Goal:** 修正完整人工校正剩余时间预填，实现账号级单按钮恢复、导航本轮队尾重试，以及截止后固定 10,000 利润门槛的两次最终筛选和日级轮空持久化。

**Architecture:** 账号恢复继续由 `special_ops` revision 临界区原子写盘；导航重试归 `round_runner` 管理，不把首次超时写入业务状态。利润截止增加持久化范围快照和目标级轮空决策，运行期查询资格仍由 `ProfitQueryControl` 管理，纯分类逻辑放入新的 `profit/cutoff.rs`。

**Tech Stack:** Rust、Tokio、Serde、Tauri 2、React 19、TypeScript、Vitest、Bun。

---

## 文件结构

- `src/components/app/special-ops-utils.ts`：完成时间转小时/分钟的纯函数。
- `src/components/app/special-ops-utils.test.ts`：剩余时间边界测试。
- `src/components/app/special-ops-types.ts`：新增账号、利润 runtime、时间轴序列化类型。
- `src/components/app/special-ops-page.tsx`：完整人工校正预填、账号卡片与时间轴人工恢复入口。
- `src/components/app/special-ops-page.test.tsx`：人工恢复 UI 与命令接线回归测试。
- `src/components/app/special-ops-profit-filter.tsx`：截止阶段与时间轴新文案。
- `src-tauri/src/special_ops/round_runner.rs`：导航首次超时队尾重试。
- `src-tauri/src/special_ops/round_planner.rs`：规则资格与截止目标资格分离。
- `src-tauri/src/special_ops/profit/model.rs`：`cutoffState` 持久化 schema、normalize 和配置更新保留规则。
- `src-tauri/src/special_ops/profit/cutoff.rs`：截止结果分类纯函数。
- `src-tauri/src/special_ops/profit/runtime.rs`：最终查询、补查和完成阶段。
- `src-tauri/src/special_ops/profit/mod.rs`：导出截止模块。
- `src-tauri/src/special_ops/mod.rs`：命令、失败映射、scheduler、查询写盘、计划和时间轴集成。
- `src-tauri/src/lib.rs`：注册新增 Tauri command。
- `README.md`、`AGENTS.md`、`droid-wiki/features/special-ops.md`：同步新增 command、schema 和行为。

执行 Rust 命令前设置独立 target，避免正在运行的开发版锁住默认 exe：

```powershell
$env:CARGO_TARGET_DIR='C:\delta-auto-tools\temp\cargo-target-manual-profit-nav'
```

### Task 1：完整人工校正继承剩余时间

**Files:**
- Modify: `src/components/app/special-ops-utils.ts`
- Modify: `src/components/app/special-ops-utils.test.ts`
- Modify: `src/components/app/special-ops-page.tsx`

- [ ] **Step 1：写失败测试**

在 `special-ops-utils.test.ts` 增加：

```ts
it("按完成时间生成剩余小时和分钟并向上取整", () => {
    const nowMs = 1_000_000;
    expect(createStationRemainingTimeDraft({finishesAtMs: nowMs + 61_001}, nowMs))
        .toEqual({hours: "0", minutes: "2"});
    expect(createStationRemainingTimeDraft({finishesAtMs: nowMs}, nowMs))
        .toEqual({hours: "", minutes: ""});
    expect(createStationRemainingTimeDraft({finishesAtMs: nowMs + 10_081 * 60_000}, nowMs))
        .toEqual({hours: "", minutes: ""});
});
```

- [ ] **Step 2：运行测试确认 RED**

```powershell
bunx vitest run src/components/app/special-ops-utils.test.ts
```

预期：`createStationRemainingTimeDraft` 未导出。

- [ ] **Step 3：实现纯函数并复用**

```ts
export function createStationRemainingTimeDraft(
    station: Pick<StationPlan, "finishesAtMs">,
    nowMs: number,
): Pick<InlineStationCorrectionDraft, "hours" | "minutes"> {
    const remaining = station.finishesAtMs !== null && station.finishesAtMs > nowMs
        ? Math.ceil((station.finishesAtMs - nowMs) / 60_000)
        : null;
    if (remaining === null || remaining > 10_080) return {hours: "", minutes: ""};
    return {hours: String(Math.floor(remaining / 60)), minutes: String(remaining % 60)};
}
```

让 `createInlineStationCorrectionDraft` 调用该函数。账号完整校正的“正在制作” `onChange` 查找当前制作台，并以 `Date.now()` 重新计算：

```tsx
const selectCorrectionCrafting = (kind: StationKind) => {
    const station = correctionAccount?.stations.find((item) => item.kind === kind)
        ?? {finishesAtMs: null};
    updateCorrection(kind, {
        state: "crafting",
        ...createStationRemainingTimeDraft(station, Date.now()),
    });
};
```

- [ ] **Step 4：运行前端定向测试确认 GREEN**

```powershell
bunx vitest run src/components/app/special-ops-utils.test.ts src/components/app/special-ops-page.test.tsx
```

预期：相关测试全部通过。

### Task 2：账号级人工恢复 schema 与 command

**Files:**
- Modify: `src-tauri/src/special_ops/mod.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1：写后端失败测试**

在 `special_ops/mod.rs` 测试模块增加：

```rust
#[test]
fn account_manual_check_only_restores_account_level_failures() {
    for status in [
        AccountStatus::NeedsManualLogin,
        AccountStatus::LoginFailed,
        AccountStatus::ManualCheckRequired,
    ] {
        let mut settings = SpecialOpsSettings::default();
        let mut selected = account("selected", status, vec![station(StationKind::Workbench, 9_000)]);
        selected.last_failure = Some(AccountFailure {
            step: "navigation.WaitStationGrid".to_string(),
            message: "步骤超时".to_string(),
            at_ms: 1,
            station_kind: None,
            ammo_target_id: None,
        });
        let stations_before = selected.stations.clone();
        settings.accounts.push(selected);
        apply_account_manual_check(&mut settings, "selected").unwrap();
        assert_eq!(settings.accounts[0].status, AccountStatus::Ready);
        assert_eq!(settings.accounts[0].stations, stations_before);
        assert_eq!(settings.accounts[0].last_failure, None);
    }

    let mut settings = SpecialOpsSettings::default();
    settings.accounts.push(account("selected", AccountStatus::Uncertain, Vec::new()));
    assert!(apply_account_manual_check(&mut settings, "selected").is_err());
}
```

- [ ] **Step 2：运行测试确认 RED**

```powershell
cargo test --manifest-path src-tauri/Cargo.toml account_manual_check_only_restores_account_level_failures
```

预期：`ManualCheckRequired` 与 `apply_account_manual_check` 不存在。

- [ ] **Step 3：实现状态与纯变更函数**

给 `AccountStatus` 增加 `ManualCheckRequired`。新增：

```rust
fn account_allows_manual_check(status: &AccountStatus) -> bool {
    matches!(
        status,
        AccountStatus::NeedsManualLogin
            | AccountStatus::LoginFailed
            | AccountStatus::ManualCheckRequired
    )
}

fn apply_account_manual_check(
    settings: &mut SpecialOpsSettings,
    account_id: &str,
) -> Result<(), String> {
    let account = settings.accounts.iter_mut()
        .find(|account| account.id == account_id)
        .ok_or_else(|| "人工检查账号不存在".to_string())?;
    if !account_allows_manual_check(&account.status) {
        return Err("当前账号状态不能通过‘已人工检查’恢复".to_string());
    }
    account.status = AccountStatus::Ready;
    account.last_failure = None;
    Ok(())
}
```

把单项制作台/子弹校正的登录阻断判断改为调用 `account_allows_manual_check`，防止新状态绕过。

- [ ] **Step 4：新增原子 command**

新增完整 revision 临界区 command：

```rust
#[tauri::command]
pub fn special_ops_confirm_account_manual_check(
    app: AppHandle,
    state: State<'_, SpecialOpsState>,
    settings_coordinator: State<'_, Arc<SettingsCoordinator>>,
    account_id: String,
    settings_revision: u64,
) -> Result<SpecialOpsBootstrap, AppError> {
    ensure_no_active_special_ops_run(&state.login_runtime)?;
    let ((settings, current_ms), revision) = settings_coordinator
        .with_expected_revision_change(
            settings_revision,
            || -> Result<(SpecialOpsSettings, i64), String> {
                ensure_no_active_special_ops_run(&state.login_runtime)?;
                let mut next = state
                    .settings
                    .lock()
                    .map_err(|_| "特勤处状态已损坏".to_string())?
                    .clone();
                apply_account_manual_check(&mut next, &account_id)?;
                let next = normalize_settings(next)?;
                save_settings(&app, &next)?;
                *state
                    .settings
                    .lock()
                    .map_err(|_| "特勤处状态已损坏".to_string())? = next.clone();
                Ok((next, now_ms()))
            },
        )
        .map_err(AppError::from)?;
    state
        .profit_runtime
        .invalidate("账号已人工检查，旧查询已取消");
    let bootstrap = build_bootstrap_with_runtime(
        settings,
        revision,
        current_ms,
        &state.login_runtime,
        &state.profit_runtime,
    )?;
    emit_state(&app, bootstrap.settings_revision, bootstrap.now_ms);
    state.round_scheduler.wake();
    Ok(bootstrap)
}
```

命令不得修改制作台、子弹运行态或 `paused`。在 `src-tauri/src/lib.rs` 的 `generate_handler![]` 注册该 command；不新增单命令 ACL。

- [ ] **Step 5：运行后端定向测试**

```powershell
cargo test --manifest-path src-tauri/Cargo.toml account_manual_check
```

预期：新增测试和既有人工校正测试通过。

### Task 3：账号卡片与时间轴单按钮恢复 UI

**Files:**
- Modify: `src/components/app/special-ops-types.ts`
- Modify: `src/components/app/special-ops-page.tsx`
- Modify: `src/components/app/special-ops-page.test.tsx`

- [ ] **Step 1：写失败测试**

增加断言：

```ts
expect(pageSource).toContain("special_ops_confirm_account_manual_check");
expect(pageSource).toContain("已人工检查");
expect(pageSource).toContain("manualCheckRequired");
```

并在类型测试夹具中加入 `manualCheckRequired`。

- [ ] **Step 2：运行测试确认 RED**

```powershell
bunx vitest run src/components/app/special-ops-page.test.tsx src/components/app/special-ops-utils.test.ts
```

- [ ] **Step 3：实现类型、标签和统一 handler**

```ts
export type AccountStatus = "ready" | "needsManualLogin" | "loginFailed"
    | "manualCheckRequired" | "uncertain" | "isolated";
```

新增 `isAccountManualCheckStatus`。统一 handler 必须先 `window.confirm`，再 `flushSettings()`，调用 `special_ops_confirm_account_manual_check`，revision 陈旧时 reload。

账号卡片：三种账号级状态只显示“已人工检查”，隐藏“人工校正制作与子弹状态”。

时间轴：三种账号级状态在每条对应任务下显示同一按钮；`uncertain` / `isolated` 继续显示现有单项或账号页处理入口。

- [ ] **Step 4：运行测试确认 GREEN**

```powershell
bunx vitest run src/components/app/special-ops-page.test.tsx src/components/app/special-ops-utils.test.ts
```

### Task 4：导航首次超时队尾重试

**Files:**
- Modify: `src-tauri/src/special_ops/round_runner.rs`
- Modify: `src-tauri/src/special_ops/mod.rs`

- [ ] **Step 1：写 round runner 失败测试**

使用三个账号计划和 FakeDriver 结果队列，断言：

```rust
#[tokio::test]
async fn navigation_timeout_retries_account_after_other_accounts() {
    let driver = FakeDriver::new(vec![
        Err(AccountRunError::navigation_timeout("navigation.WaitModeReady")),
        Ok(AccountRunSuccess { processed_stations: 1 }),
        Ok(AccountRunSuccess { processed_stations: 1 }),
        Ok(AccountRunSuccess { processed_stations: 1 }),
    ], false);
    let result = run_round(&driver, &three_account_plan(), Arc::new(AtomicBool::new(false))).await;
    assert_eq!(result.stop, RoundStop::Completed);
    assert_eq!(driver.actions(), ["run:first", "run:second", "run:third", "run:first", "close-game"]);
    assert!(!driver.actions().iter().any(|action| action == "persist-account:first"));
}
```

再写第二次超时断言只持久化一次；`AccountRunError::system` 仍全面暂停。

- [ ] **Step 2：运行测试确认 RED**

```powershell
cargo test --manifest-path src-tauri/Cargo.toml special_ops::round_runner::tests
```

- [ ] **Step 3：给错误增加显式分类**

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub(crate) enum AccountRunErrorKind {
    Regular,
    NavigationTimedOut,
}
```

`AccountRunError` 增加 `kind`；现有构造器固定 `Regular`，新增 `navigation_timeout(step)`。`map_round_navigation_result(TimedOut)` 使用新构造器，禁止依据 message 文本判断。

- [ ] **Step 4：把 for-loop 改为本轮队列**

使用：

```rust
struct QueuedAccount {
    task: AccountRoundTask,
    navigation_retries: u8,
}
```

从 `plan.accounts` 构造 `VecDeque`。首次 `NavigationTimedOut` 时将重试数改为 1 后 `push_back`，不调用 `persist_account_failure`；第二次进入既有账号失败持久化分支。每次 attempt 后仍检查紧急停止和 `pause_requested`。

- [ ] **Step 5：持久化第二次超时为“需人工检查”**

`apply_round_account_failure` 对 `AccountRunErrorKind::NavigationTimedOut` 映射 `AccountStatus::ManualCheckRequired`，不修改任何制作台状态。

- [ ] **Step 6：运行导航与 round 测试确认 GREEN**

```powershell
cargo test --manifest-path src-tauri/Cargo.toml special_ops::round_runner::tests
cargo test --manifest-path src-tauri/Cargo.toml round_navigation
```

### Task 5：截止范围与轮空持久化模型

**Files:**
- Modify: `src-tauri/src/special_ops/profit/model.rs`
- Modify: `src-tauri/src/special_ops/mod.rs`

- [ ] **Step 1：写 serde/normalize 失败测试**

覆盖旧 JSON 缺少新字段、同日去重、跨日清理、配置保存不清除同日决策：

```rust
#[test]
fn cutoff_state_defaults_and_preserves_same_day_decisions() {
    let mut filter: ProfitFilterSettings = serde_json::from_str(
        r#"{"enabled":true,"cutoffTime":"17:00","rules":[],"audits":[]}"#,
    ).unwrap();
    assert_eq!(filter.cutoff_state, None);
    normalize_profit_settings(&mut filter).unwrap();
}
```

- [ ] **Step 2：运行测试确认 RED**

```powershell
cargo test --manifest-path src-tauri/Cargo.toml cutoff_state
```

- [ ] **Step 3：新增 camelCase schema**

```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub struct ProfitCutoffState {
    pub day: String,
    pub targets: Vec<ProfitCutoffTarget>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub struct ProfitCutoffTarget {
    pub account_id: String,
    pub target_id: String,
    pub rule_id: Option<String>,
    pub skip_reason: Option<ProfitCutoffSkipReason>,
    pub decided_at_ms: Option<i64>,
}
```

`ProfitCutoffSkipReason` 使用 `BelowThreshold | QueryUnavailable | Unconfigured`。`ProfitFilterSettings` 增加 `#[serde(default)] pub cutoff_state: Option<ProfitCutoffState>`。

normalize：trim、按 `(account_id,target_id)` 去重、清除无效空 ID；自然日变化由调度入口显式替换，不在无时钟的 normalize 内猜当前日期。`apply_profit_configuration` 必须保留同日 `cutoff_state`，不得因修改规则或绑定清除。

- [ ] **Step 4：实现截止范围冻结纯函数**

在 `special_ops/mod.rs` 从当前 `Ready` 账号、启用点击点目标和当天子弹状态生成 `ProfitCutoffTarget`。目标未绑定规则、规则不存在或 KKRB 名称为空时立即写 `Unconfigured`；新目标不追加到同日已存在的范围。

- [ ] **Step 5：运行模型测试确认 GREEN**

```powershell
cargo test --manifest-path src-tauri/Cargo.toml special_ops::profit::model::tests
```

### Task 6：截止查询分类与两次状态机

**Files:**
- Create: `src-tauri/src/special_ops/profit/cutoff.rs`
- Modify: `src-tauri/src/special_ops/profit/mod.rs`
- Modify: `src-tauri/src/special_ops/profit/runtime.rs`
- Modify: `src-tauri/src/special_ops/mod.rs`

- [ ] **Step 1：写截止分类失败测试**

`cutoff.rs` 定义固定值和纯分类：

```rust
pub(crate) const FINAL_MINIMUM_PROFIT: u64 = 10_000;
pub(crate) const FINAL_RETRY_DELAY_MS: i64 = 5 * 60_000;

pub(crate) struct CutoffClassification {
    pub qualified_rule_ids: HashSet<String>,
    pub retry_rule_ids: HashSet<String>,
    pub skipped: HashMap<String, ProfitCutoffSkipReason>,
}
```

测试：首查 `BelowThreshold` 直接 skip；首查 `TargetMissing/SourceFailure` retry；第二查所有非 `Qualified` skip；阈值固定 10,000。

- [ ] **Step 2：运行测试确认 RED**

```powershell
cargo test --manifest-path src-tauri/Cargo.toml special_ops::profit::cutoff
```

- [ ] **Step 3：实现纯分类并导出模块**

不得读取系统时间、设置或 runtime；输入仅 audits 与 attempt。查询前复制规则并把 `minimum_profit` 改为 `FINAL_MINIMUM_PROFIT`，使现有 query adapter 产出正确 audit threshold。

- [ ] **Step 4：扩展 runtime 显式阶段**

删除 `CutoffBypass`，增加：

```rust
CutoffQuerying,
WaitingCutoffRetry,
CutoffComplete,
```

`QueryLease` 增加 `mode: ProfitQueryMode`：`Regular` 或 `Cutoff { attempt: 1 | 2 }`。新增 `begin_cutoff_query` 与 `complete_cutoff_query_at_revision`；第一查只允许进入一次 5 分钟补查，第二查完成后 `next_query_at_ms = None`。

- [ ] **Step 5：改 scheduler 查询写盘**

`execute_profit_query_action` 在截止前走原分支；截止后：

1. 原子建立当天 `cutoff_state`，成功写盘后更新 lease revision。
2. 查询范围内尚未决定的规则；第二次只查首查 transient 规则。
3. 同一 coordinator 临界区写 audits 与每个目标的 `skip_reason/decided_at_ms`。
4. 成功写盘后调用 `complete_cutoff_query_at_revision`。
5. 首查 transient 设置 `next_query_at_ms = completed + 5min`；第二查结束进入 `CutoffComplete`。

- [ ] **Step 6：运行截止 runtime 测试确认 GREEN**

```powershell
cargo test --manifest-path src-tauri/Cargo.toml special_ops::profit::runtime::tests
cargo test --manifest-path src-tauri/Cargo.toml cutoff
```

### Task 7：round gate、scheduler 与 24 小时时间轴

**Files:**
- Modify: `src-tauri/src/special_ops/round_planner.rs`
- Modify: `src-tauri/src/special_ops/mod.rs`
- Modify: `src/components/app/special-ops-types.ts`
- Modify: `src/components/app/special-ops-profit-filter.tsx`
- Modify: `src/components/app/special-ops-page.tsx`
- Modify: relevant `*.test.ts(x)` files

- [ ] **Step 1：写 round planner 失败测试**

把旧 `CutoffBypass` 测试替换为：正常查询按规则 ID 放行；截止查询只能放行 `cutoff_state.targets` 与达标规则交集；轮空或范围外目标不得进入 `due_accounts`。

- [ ] **Step 2：运行测试确认 RED**

```powershell
cargo test --manifest-path src-tauri/Cargo.toml special_ops::round_planner::tests
```

- [ ] **Step 3：拆分利润 gate**

```rust
pub(crate) enum AmmoProfitGate {
    Disabled,
    QualifiedRules(HashSet<String>),
    QualifiedTargets(HashSet<(String, String)>),
    DisplayOnly,
}
```

截止 runtime 用“范围快照 ∩ 达标规则”生成 `(account_id,target_id)`，禁止只凭同一规则放行截止后新增目标。

- [ ] **Step 4：更新 scheduler 下一动作**

截止时间本身必须产生 `QueryProfit`，不能产生无门槛 `LaunchRound`。首查 transient 后下一 query 固定为 5 分钟；低利润或最终失败仅写轮空，不启动 round。满足门槛时先消费同 generation 资格再启动 round，启动失败仍回滚该 generation。

- [ ] **Step 5：更新时间轴投影**

TypeScript/Rust 类型替换：

```ts
type ProfitRuntimePhase = "disabled" | "waitingExchange" | "querying"
    | "waitingNextQuery" | "activeRound" | "cutoffQuerying"
    | "waitingCutoffRetry" | "cutoffComplete" | "paused";

type TimelineProfitState = "waitingExchange" | "waitingQuery" | "unconfigured"
    | "qualified" | "activeRound" | "cutoffQuerying"
    | "waitingCutoffRetry" | "cutoffSkipped";
```

轮空任务不进入 `due_accounts`，但仍以截止时间锚点留在 `timelineTasks`，显示“今日轮空”；补查目标显示“5 分钟后最终补查”。删除所有“截止后忽略利润”文案。

- [ ] **Step 6：运行调度与前端定向测试**

```powershell
cargo test --manifest-path src-tauri/Cargo.toml profit_timeline
cargo test --manifest-path src-tauri/Cargo.toml special_ops::round_scheduler::tests
bunx vitest run src/components/app/special-ops-profit-utils.test.ts src/components/app/special-ops-profit-filter.test.tsx src/components/app/special-ops-page.test.tsx
```

### Task 8：文档同步、索引与全量验证

**Files:**
- Modify: `README.md`
- Modify: `AGENTS.md`
- Modify: `droid-wiki/features/special-ops.md`
- Review: `docs/superpowers/specs/2026-08-06-特勤处人工恢复利润截止与导航重试-design.md`

- [ ] **Step 1：同步文档**

记录：

- 新 command `special_ops_confirm_account_manual_check`。
- `manualCheckRequired` 与 `profitFilter.cutoffState` schema。
- 导航本轮队尾重试一次。
- 截止固定 10,000 门槛、首次 transient 5 分钟补查一次、当天轮空。
- 完整人工校正剩余时间预填。

- [ ] **Step 2：刷新 CodeGraph**

```powershell
codegraph sync
```

预期：索引成功，无 fatal error。

- [ ] **Step 3：运行格式和差异检查**

```powershell
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
git diff --check
```

预期：exit 0；允许仓库既存 LF/CRLF warning，不允许 whitespace error。

- [ ] **Step 4：运行全量质量门禁**

```powershell
$env:CARGO_TARGET_DIR='C:\delta-auto-tools\temp\cargo-target-manual-profit-nav'
bun run check
```

预期：TypeScript、Vitest、coverage、Rust fmt、Clippy `-D warnings`、Rust tests 全部通过。

- [ ] **Step 5：最终状态核验**

```powershell
git status --short --branch
git diff --stat
```

确认仅保留工作树改动；不执行 `git add`、`git commit`、merge 或 push。
