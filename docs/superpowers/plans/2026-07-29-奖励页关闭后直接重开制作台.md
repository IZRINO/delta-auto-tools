# 奖励页关闭后直接重开制作台 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 奖励页按 `Space` 关闭后直接点击当前制作台，不等待 `game.stationGrid` 识别。

**Architecture:** 保留 `run_craft_station` 与 `open_station` 边界。只删奖励分支中间的 `game.stationGrid` stage 和 `wait_ready`；第二次 `open_station` 继续负责点击当前台、识别制作列表及三次重试。

**Tech Stack:** Rust、Tokio、Cargo test、Tauri 2

---

### Task 1: 用回归测试锁定奖励分支动作顺序

**Files:**
- Modify: `src-tauri/src/special_ops/craft_runtime.rs:802`
- Test: `src-tauri/src/special_ops/craft_runtime.rs`

- [ ] **Step 1: 修改现有测试，使其要求不等待四制作台页面**

将测试改名为 `reward_branch_closes_reward_then_reopens_same_station_without_grid_wait`，保留一次 `Space` 与两次当前制作台点击断言，并把原有正向断言：

```rust
assert!(actions.contains(&"wait:game.stationGrid".to_string()));
```

替换为：

```rust
assert!(!actions.contains(&"wait:game.stationGrid".to_string()));
let space_index = actions.iter().position(|action| action == "space").unwrap();
let second_station_index = actions
    .iter()
    .enumerate()
    .filter(|(_, action)| action.as_str() == "station:craft.station.pharmacy")
    .nth(1)
    .map(|(index, _)| index)
    .unwrap();
assert!(space_index < second_station_index);
```

- [ ] **Step 2: 运行测试并确认 RED**

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml --lib reward_branch_closes_reward_then_reopens_same_station_without_grid_wait
```

Expected: FAIL，失败原因为动作列表仍包含 `wait:game.stationGrid`。

- [ ] **Step 3: 删除奖励分支的中间识别**

从 `run_craft_station` 的 `StationOpenResult::Reward` 分支删除：

```rust
driver
    .update_stage(LoginRunStatus::Waiting, "正在确认已返回四制作台页面")
    .map_err(|message| failure_after_input("game.stationGrid", &message))?;
driver
    .wait_ready("game.stationGrid", Arc::clone(&cancelled))
    .await
    .map_err(|message| failure_after_input("game.stationGrid", &message))?;
```

按 `Space` 后立即调用现有：

```rust
open_station(
    driver,
    &station_key,
    &list_key,
    false,
    Arc::clone(&cancelled),
)
.await?;
```

- [ ] **Step 4: 运行定向测试并确认 GREEN**

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml --lib special_ops::craft_runtime::negative_guard_tests
```

Expected: 制作负向守卫测试全部通过，0 failed。

### Task 2: 同步行为文档并验证

**Files:**
- Modify: `droid-wiki/features/special-ops.md`
- Modify: `docs/superpowers/plans/2026-07-29-奖励页关闭后直接重开制作台.md`

- [ ] **Step 1: 更新 wiki 的奖励分支说明**

把奖励分支写成：

```text
识别奖励页后按 Space，随后直接点击当前制作台并识别当前台制作列表；不等待 game.stationGrid，也不再次检查 craft.inProgress.<station>。
```

- [ ] **Step 2: 刷新 CodeGraph**

Run:

```powershell
codegraph sync
```

Expected: exit code 0。

- [ ] **Step 3: 运行 Rust special_ops 测试**

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml --lib special_ops::tests
cargo test --manifest-path src-tauri/Cargo.toml --lib special_ops::craft_runtime::negative_guard_tests
```

Expected: 两条命令均 exit code 0，0 failed。

- [ ] **Step 4: 检查 diff**

Run:

```powershell
git diff --check
git status --short --branch
```

Expected: `git diff --check` exit code 0；只报告当前工作树状态，不清理既有改动。

- [ ] **Step 5: 等待实机复测后再提交生产代码**

用户关闭并重启开发版，选择可收取制作台启动制作试运行。预期奖励页按 `Space` 后不出现“正在确认已返回四制作台页面”，而是直接点击当前制作台并进入制作列表。实机确认前不提交生产代码。
