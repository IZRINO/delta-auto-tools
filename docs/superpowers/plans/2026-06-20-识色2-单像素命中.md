# 识色2 · 单像素命中 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在保留现有「识色1（区域平均色判定）」的基础上，为识色卡片新增「识色2（框选范围内单像素命中即触发）」匹配方式，与既有跨探针聚合模式（All/Any）正交（Issue #63）。

**Architecture:** 在 `AudioCard` 新增卡片级字段 `color_match_method: ColorMatchMethod`（Average=识色1/AnyPixel=识色2，默认 Average 零迁移）。抽取统一单探针判定助手 `probe_hit(screenshot, probe, method, count_only) -> ProbeHit`，watcher 路径 `count_only=true`（anyPixel 命中即早退，性能优先），test 路径 `count_only=false`（全扫拿命中数与最近像素，调试优先）。`match_color_probes` 与 `audio_test_color_match` 共用此助手，消除现有 average 判定重复。

**Tech Stack:** Rust（image crate 现有依赖，无新增）、Tauri command、React + TypeScript + shadcn/ui、Vitest。

## Global Constraints

- Rust 对外序列化结构体必须 `#[serde(rename_all = "camelCase")]`。`ColorMatchMethod::AnyPixel` 序列化为 `"anyPixel"`，前端 union 必须匹配 `"average" | "anyPixel"`。
- 缺省字段必须向后兼容：旧 JSON 无 `colorMatchMethod` 时反序列化为 `Average`，前端缺省回退 `"average"`。
- 前端只收到 camelCase 字段；`ColorProbeTestResult` 新增 `matchingPixelCount` 为旧字段不删不改语义的追加。
- UI 仅使用 shadcn/ui + Tailwind 工具类，禁止新增 `.desktop-*`/`.tactical-*` 自定义类。
- 运行单个 Rust 测试：`cargo test --manifest-path src-tauri/Cargo.toml <test_name>`
- 运行单个前端测试：`bunx vitest run src/components/app/audio-utils.test.ts`
- Commit message 使用中文。

---

## File Structure

**修改：**

- `src-tauri/src/audio/types.rs` — 新增 `ColorMatchMethod` 枚举；`AudioCard` 增 `color_match_method` 字段
- `src-tauri/src/audio/watcher.rs` — 新增 `PixelScanResult`/`ProbeHit` 结构、`scan_region_for_color`/`probe_hit` 函数；`match_color_probes` 加 `method` 参数；`run_color_watcher` 透传 method
- `src-tauri/src/audio/mod.rs` — `ColorProbeTestResult` 增 `matching_pixel_count`；`audio_test_color_match` 读取 method 并复用 `probe_hit`
- `src/components/app/audio-types.ts` — 新增 `ColorMatchMethod` 类型；`AudioCard`/`AudioCardForm`/`DEFAULT_AUDIO_CARD` 增字段
- `src/components/app/audio-utils.ts` — `cardToForm`/`parseCardForm` 透传 `colorMatchMethod`
- `src/components/app/audio-page.tsx` — 新增「匹配方式」下拉；原「匹配模式」改名「聚合模式」；测试 toast 追加命中像素数
- `src/components/app/audio-utils.test.ts` — `colorMatchMethod` roundtrip / 缺省回退用例

**测试（同文件 `#[cfg(test)] mod tests`）：**

- `src-tauri/src/audio/watcher.rs` 内测试模块 — `scan_region_for_color`/`probe_hit`/`match_color_probes`(anyPixel) 用例
- `src-tauri/src/audio/types.rs` 内测试模块 — `ColorMatchMethod` serde 用例

---

## Task 1: Rust 数据模型 — ColorMatchMethod 枚举与 AudioCard 字段

**Files:**
- Modify: `src-tauri/src/audio/types.rs`
- Test: `src-tauri/src/audio/types.rs` 内 `#[cfg(test)] mod tests`

**Interfaces:**
- Consumes: 无（新枚举自包含）
- Produces: `enum ColorMatchMethod { Average, AnyPixel }`（Default=Average，serde camelCase → `"average"`/`"anyPixel"`）；`AudioCard.color_match_method: ColorMatchMethod` 字段

- [ ] **Step 1: 写失败测试 — 缺省回退 Average**

在 `src-tauri/src/audio/types.rs` 的 `mod tests` 末尾追加：

```rust
    #[test]
    fn color_match_method_default_is_average() {
        // AudioCard 缺省 color_match_method 应为 Average（向后兼容旧 JSON）
        let json = r#"{"id":"c1","name":"测试","triggerMode":"colorWatch"}"#;
        let card: AudioCard = serde_json::from_str(json).unwrap();
        assert_eq!(card.color_match_method, ColorMatchMethod::Average);
    }

    #[test]
    fn color_match_method_anypixel_roundtrip() {
        let json = r#"{"id":"c1","name":"单像素","triggerMode":"colorWatch","colorMatchMethod":"anyPixel"}"#;
        let card: AudioCard = serde_json::from_str(json).unwrap();
        assert_eq!(card.color_match_method, ColorMatchMethod::AnyPixel);
        let reserialized = serde_json::to_string(&card).unwrap();
        assert!(reserialized.contains("\"colorMatchMethod\":\"anyPixel\""), "应序列化为 camelCase anyPixel，实际 {reserialized}");
    }

    #[test]
    fn color_match_method_default_value_is_average_enum() {
        assert_eq!(ColorMatchMethod::default(), ColorMatchMethod::Average);
    }
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml color_match_method`
Expected: 编译失败 — `ColorMatchMethod` 未定义 / `AudioCard` 无 `color_match_method` 字段

- [ ] **Step 3: 写最小实现 — 枚举 + 字段**

在 `src-tauri/src/audio/types.rs` 的 `ColorMatchMode` 枚举定义之后追加：

```rust
/// 单个探针的匹配方式
/// - Average（识色1）：取框选区域平均 RGB，与目标色距离 ≤ 容差即命中
/// - AnyPixel（识色2）：框选区域内有任意一个像素与目标色距离 ≤ 容差即命中
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq, Default)]
#[serde(rename_all = "camelCase")]
pub enum ColorMatchMethod {
    #[default]
    Average,
    AnyPixel,
}
```

在 `AudioCard` 结构体的 `color_match_mode` 字段之后追加（注意上一字段尾逗号）：

```rust
    // 识色匹配方式：Average=平均色判定；AnyPixel=单像素命中。缺省 Average（识色1，向后兼容）
    #[serde(default)]
    pub color_match_method: ColorMatchMethod,
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml color_match_method`
Expected: PASS（3 个测试）

- [ ] **Step 5: 编译检查全量**

Run: `cargo check --manifest-path src-tauri/Cargo.toml`
Expected: 编译失败——`watcher.rs::match_color_probes` 现有调用签名（line 285）尚未传 method。此为预期，Task 2/3 修复。

> 注：若此处编译错误阻塞其它验证，可临时在 `run_color_watcher` 调用处补 `ColorMatchMethod::Average` 占位，但 Task 3 会改为真实透传。建议直接进入 Task 2 先补 watcher 逻辑再编译。

- [ ] **Step 6: 提交**

```bash
git add src-tauri/src/audio/types.rs
git commit -m "feat(audio): 新增 ColorMatchMethod 枚举与 color_match_method 字段（Issue #63）"
```

---

## Task 2: Rust 判定逻辑 — scan_region_for_color 与 probe_hit

**Files:**
- Modify: `src-tauri/src/audio/watcher.rs`
- Test: `src-tauri/src/audio/watcher.rs` 内 `mod tests`

**Interfaces:**
- Consumes: `crate::audio::types::ColorMatchMethod`、既有 `average_region_rgb`、`color_distance`、`ColorProbe`
- Produces:
  - `struct PixelScanResult { matching_count, nearest_color, nearest_distance }`
  - `struct ProbeHit { matched, sampled_color, distance, matching_pixel_count }`
  - `fn scan_region_for_color(img, target, tolerance, count_only) -> PixelScanResult`
  - `fn probe_hit(screenshot, probe, method, count_only) -> ProbeHit`

- [ ] **Step 1: 写失败测试 — scan_region_for_color 单像素命中**

在 `watcher.rs` 的 `mod tests` 末尾追加（注意已有 `use` 行包含 `image::{DynamicImage, ... RgbaImage}`）：

```rust
    #[test]
    fn any_pixel_hit_single_pixel() {
        // 4×4 全黑，放 1 个目标色像素 → matching_count >= 1 且 nearest_color == 目标色
        let mut img = RgbaImage::new(4, 4);
        for y in 0..4 {
            for x in 0..4 {
                img.put_pixel(x, y, Rgba([0, 0, 0, 255]));
            }
        }
        img.put_pixel(2, 1, Rgba([200, 100, 50, 255]));
        let dyn_img = DynamicImage::ImageRgba8(img);
        let result = scan_region_for_color(&dyn_img, [200, 100, 50], 10, false);
        assert!(result.matching_count >= 1, "应至少命中 1 像素");
        assert_eq!(result.nearest_color, [200, 100, 50], "最近像素应为目标色");
    }

    #[test]
    fn any_pixel_no_hit_returns_nearest() {
        // 全黑图，目标色为白 → 无命中，nearest 为全图最近像素（黑）
        let img = RgbaImage::from_pixel(3, 3, Rgba([0, 0, 0, 255]));
        let dyn_img = DynamicImage::ImageRgba8(img);
        let result = scan_region_for_color(&dyn_img, [255, 255, 255], 10, false);
        assert_eq!(result.matching_count, 0, "无命中像素");
        assert_eq!(result.nearest_color, [0, 0, 0], "最近像素为黑");
    }

    #[test]
    fn any_pixel_early_exit_count_only() {
        // 2×2 全为目标色，count_only=true → matching_count == 1（早退）
        let img = RgbaImage::from_pixel(2, 2, Rgba([200, 100, 50, 255]));
        let dyn_img = DynamicImage::ImageRgba8(img);
        let result = scan_region_for_color(&dyn_img, [200, 100, 50], 10, true);
        assert_eq!(result.matching_count, 1, "count_only=true 命中后应早退，count 为 1");
    }

    #[test]
    fn any_pixel_tolerance_boundary() {
        // 像素距离恰等于容差 → 命中（<= 边界）
        // 目标 [100,100,100]，像素 [110,100,100] 距离 10，容差 10 → 命中
        let mut img = RgbaImage::new(2, 1);
        img.put_pixel(0, 0, Rgba([110, 100, 100, 255]));
        img.put_pixel(1, 0, Rgba([0, 0, 0, 255]));
        let dyn_img = DynamicImage::ImageRgba8(img);
        let result = scan_region_for_color(&dyn_img, [100, 100, 100], 10, false);
        assert!(result.matching_count >= 1, "距离恰等于容差应命中");
    }
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml any_pixel`
Expected: 编译失败 — `scan_region_for_color` 未定义

- [ ] **Step 3: 写最小实现 — 结构 + scan_region_for_color**

在 `watcher.rs` 的 `average_region_rgb` 函数之后追加：

```rust
/// 像素扫描结果
#[derive(Debug, Clone)]
pub(crate) struct PixelScanResult {
    /// 命中像素数
    pub matching_count: usize,
    /// 最接近目标的像素色（无命中时为全图最近像素）
    pub nearest_color: [u8; 3],
    /// nearest_color 与目标的欧氏距离
    pub nearest_distance: f32,
}

/// 扫描区域，返回命中像素数与最接近目标色的像素。
///
/// - `count_only=true`：命中首个像素即早退，`matching_count` 至多为 1（watcher 用，性能优先）。
/// - `count_only=false`：全扫，`matching_count` 为真实命中数（test 用，调试优先）。
///
/// `nearest_color`/`nearest_distance` 始终为全图最接近目标的像素（命中时即目标色本身，距离 0）。
/// alpha < 128 的透明像素跳过。
pub(crate) fn scan_region_for_color(
    img: &image::DynamicImage,
    target: [u8; 3],
    tolerance: f32,
    count_only: bool,
) -> PixelScanResult {
    let rgba = img.to_rgba8();
    let (w, h) = rgba.dimensions();
    let mut matching_count: usize = 0;
    let mut nearest_color: [u8; 3] = [0, 0, 0];
    let mut nearest_distance: f32 = f32::INFINITY;

    for y in 0..h {
        for x in 0..w {
            let p = rgba.get_pixel(x, y);
            if p[3] < 128 {
                continue;
            }
            let c = [p[0], p[1], p[2]];
            let dist = color_distance(c, target);
            if dist < nearest_distance {
                nearest_distance = dist;
                nearest_color = c;
            }
            if dist <= tolerance {
                matching_count += 1;
                if count_only {
                    // 早退：nearest 已更新为目标色（距离 0）
                    return PixelScanResult { matching_count, nearest_color, nearest_distance };
                }
            }
        }
    }

    if nearest_distance == f32::INFINITY {
        // 全透明或空图
        nearest_distance = 0.0;
    }

    PixelScanResult { matching_count, nearest_color, nearest_distance }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml any_pixel`
Expected: PASS（4 个测试）

- [ ] **Step 5: 写失败测试 — probe_hit average/anyPixel 差异**

在 `mod tests` 追加：

```rust
    #[test]
    fn probe_hit_average_uses_region_avg() {
        // 2×2：一半红一半黑，平均色 ≈ [127,0,0]，目标 [127,0,0] 容差 5 → average 命中
        let mut img = RgbaImage::new(2, 2);
        img.put_pixel(0, 0, Rgba([255, 0, 0, 255]));
        img.put_pixel(1, 0, Rgba([0, 0, 0, 255]));
        img.put_pixel(0, 1, Rgba([255, 0, 0, 255]));
        img.put_pixel(1, 1, Rgba([0, 0, 0, 255]));
        let dyn_img = DynamicImage::ImageRgba8(img);
        let probe = ColorProbe {
            region: Some(RegionRect { x: 0, y: 0, width: 2, height: 2 }),
            target_color: [127, 0, 0],
            tolerance: 5,
        };
        let hit = probe_hit(&dyn_img, &probe, ColorMatchMethod::Average, false);
        assert!(hit.matched, "average 模式平均色应命中");
        assert_eq!(hit.matching_pixel_count, 0, "average 模式 matching_pixel_count 恒 0");
    }

    #[test]
    fn probe_hit_any_pixel_finds_single_pixel() {
        // 同一图：average 未中（平均色距目标远），anyPixel 命中（存在红像素）
        let mut img = RgbaImage::new(2, 2);
        img.put_pixel(0, 0, Rgba([255, 0, 0, 255]));
        img.put_pixel(1, 0, Rgba([0, 0, 0, 255]));
        img.put_pixel(0, 1, Rgba([255, 0, 0, 255]));
        img.put_pixel(1, 1, Rgba([0, 0, 0, 255]));
        let dyn_img = DynamicImage::ImageRgba8(img);
        let probe = ColorProbe {
            region: Some(RegionRect { x: 0, y: 0, width: 2, height: 2 }),
            target_color: [255, 0, 0],
            tolerance: 10,
        };
        let avg_hit = probe_hit(&dyn_img, &probe, ColorMatchMethod::Average, false);
        assert!(!avg_hit.matched, "average 模式平均色 [127,0,0] 距 [255,0,0] 远，应未中");
        let any_hit = probe_hit(&dyn_img, &probe, ColorMatchMethod::AnyPixel, false);
        assert!(any_hit.matched, "anyPixel 模式存在红像素应命中");
        assert!(any_hit.matching_pixel_count >= 1, "anyPixel 命中数应 >= 1");
    }
```

> 注意：`probe_hit` 引用 `ColorMatchMethod`，需在 `mod tests` 的 `use crate::audio::types::{ColorMatchMode, ColorProbe};` 行补入 `ColorMatchMethod`。Step 6 实现时一并改。

- [ ] **Step 6: 运行测试确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml probe_hit`
Expected: 编译失败 — `probe_hit` 未定义；`ColorMatchMethod` 未 import

- [ ] **Step 7: 写最小实现 — ProbeHit + probe_hit**

先修改 `mod tests` 的 use 行，将：
```rust
    use crate::audio::types::{ColorMatchMode, ColorProbe};
```
改为：
```rust
    use crate::audio::types::{ColorMatchMode, ColorMatchMethod, ColorProbe};
```

在 `scan_region_for_color` 函数之后追加：

```rust
/// 单探针判定结果
#[derive(Debug, Clone)]
pub(crate) struct ProbeHit {
    /// 是否命中
    pub matched: bool,
    /// average:区域平均色；anyPixel:最近像素
    pub sampled_color: [u8; 3],
    /// sampled_color 与目标色的欧氏距离
    pub distance: f32,
    /// anyPixel 命中像素数；average 恒 0
    pub matching_pixel_count: usize,
}

/// 单探针判定：按 method 计算 sampled 与目标色的距离，判定是否命中。
///
/// - `count_only=true`：anyPixel 命中即早退（watcher 用，性能优先）。
/// - `count_only=false`：全扫拿真实命中数与最近像素（test 用，调试优先）。
pub(crate) fn probe_hit(
    screenshot: &image::DynamicImage,
    probe: &crate::audio::types::ColorProbe,
    method: crate::audio::types::ColorMatchMethod,
    count_only: bool,
) -> ProbeHit {
    match method {
        crate::audio::types::ColorMatchMethod::Average => {
            let avg = average_region_rgb(screenshot);
            let dist = color_distance(avg, probe.target_color);
            ProbeHit {
                matched: dist <= probe.tolerance as f32,
                sampled_color: avg,
                distance: dist,
                matching_pixel_count: 0,
            }
        }
        crate::audio::types::ColorMatchMethod::AnyPixel => {
            let scan = scan_region_for_color(screenshot, probe.target_color, probe.tolerance as f32, count_only);
            ProbeHit {
                matched: scan.matching_count > 0,
                sampled_color: scan.nearest_color,
                distance: scan.nearest_distance,
                matching_pixel_count: scan.matching_count,
            }
        }
    }
}
```

- [ ] **Step 8: 运行测试确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml probe_hit`
Expected: PASS（2 个测试）

- [ ] **Step 9: 提交**

```bash
git add src-tauri/src/audio/watcher.rs
git commit -m "feat(audio): 新增 scan_region_for_color 与 probe_hit 统一判定（Issue #63）"
```

---

## Task 3: Rust watcher — match_color_probes 接 method 参数

**Files:**
- Modify: `src-tauri/src/audio/watcher.rs:285`（调用点）、`:104-119`（spawn 处）、`:229`（函数签名）、`:758-779`（定义）
- Test: `src-tauri/src/audio/watcher.rs` 内 `mod tests`

**Interfaces:**
- Consumes: Task 2 的 `probe_hit`、Task 1 的 `ColorMatchMethod`
- Produces: `match_color_probes` 签名 `(screenshots, probes, mode, method) -> ColorMatchResult`；`run_color_watcher` 透传 method

- [ ] **Step 1: 写失败测试 — anyPixel mode 聚合**

在 `mod tests` 追加：

```rust
    #[test]
    fn match_color_probes_any_pixel_method_hits() {
        // 单探针：全黑图中央 1 红像素，average 未中、anyPixel 命中
        let mut img = RgbaImage::new(3, 3);
        for y in 0..3 {
            for x in 0..3 {
                img.put_pixel(x, y, Rgba([0, 0, 0, 255]));
            }
        }
        img.put_pixel(1, 1, Rgba([255, 0, 0, 255]));
        let screenshots = vec![DynamicImage::ImageRgba8(img)];
        let probes = vec![ColorProbe {
            region: Some(RegionRect { x: 0, y: 0, width: 3, height: 3 }),
            target_color: [255, 0, 0],
            tolerance: 10,
        }];
        let avg = match_color_probes(&screenshots, &probes, ColorMatchMode::All, ColorMatchMethod::Average);
        assert!(!avg.matched, "average 模式平均色为黑，距红远，未中");
        let any = match_color_probes(&screenshots, &probes, ColorMatchMode::All, ColorMatchMethod::AnyPixel);
        assert!(any.matched, "anyPixel 模式存在红像素应命中");
        assert_eq!(any.hit_count, 1);
    }

    #[test]
    fn match_color_probes_any_pixel_combined_with_mode_all() {
        // 两探针：第一命中、第二未中 → mode=All 不触发，mode=Any 触发
        let mut img1 = RgbaImage::new(2, 2);
        for y in 0..2 {
            for x in 0..2 {
                img1.put_pixel(x, y, Rgba([255, 0, 0, 255]));
            }
        }
        let img2 = RgbaImage::from_pixel(2, 2, Rgba([0, 0, 0, 255]));
        let screenshots = vec![
            DynamicImage::ImageRgba8(img1),
            DynamicImage::ImageRgba8(img2),
        ];
        let probes = vec![
            ColorProbe { region: Some(RegionRect { x: 0, y: 0, width: 2, height: 2 }), target_color: [255, 0, 0], tolerance: 10 },
            ColorProbe { region: Some(RegionRect { x: 0, y: 0, width: 2, height: 2 }), target_color: [255, 0, 0], tolerance: 10 },
        ];
        let all = match_color_probes(&screenshots, &probes, ColorMatchMode::All, ColorMatchMethod::AnyPixel);
        assert!(!all.matched, "mode=All 第二探针未中，不触发");
        assert_eq!(all.hit_count, 1);
        let any = match_color_probes(&screenshots, &probes, ColorMatchMode::Any, ColorMatchMethod::AnyPixel);
        assert!(any.matched, "mode=Any 任一命中即触发");
    }
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml match_color_probes_any_pixel`
Expected: 编译失败 — `match_color_probes` 签名不接第 4 参数

- [ ] **Step 3: 改实现 — match_color_probes 加 method 参数**

将 `watcher.rs:758` 处的 `match_color_probes` 定义改为（注意签名加 `method` 参数，循环体改用 `probe_hit`）：

```rust
/// 对一组已截取的区域图像与对应探针做颜色匹配，按 mode 聚合
pub(crate) fn match_color_probes(
    screenshots: &[image::DynamicImage],
    probes: &[ColorProbe],
    mode: ColorMatchMode,
    method: ColorMatchMethod,
) -> ColorMatchResult {
    if probes.is_empty() || screenshots.len() < probes.len() {
        return ColorMatchResult { matched: false, hit_count: 0 };
    }
    let mut hit_count = 0usize;
    for (i, probe) in probes.iter().enumerate() {
        if probe_hit(&screenshots[i], probe, method.clone(), true).matched {
            hit_count += 1;
        }
    }
    let matched = match mode {
        ColorMatchMode::All => hit_count == probes.len(),
        ColorMatchMode::Any => hit_count > 0,
    };
    ColorMatchResult { matched, hit_count }
}
```

> 注意：函数体顶部 `use crate::audio::types::{ColorMatchMode, ColorProbe};`（line 16）需补入 `ColorMatchMethod`。修改 line 16：
> ```rust
> use crate::audio::types::{ColorMatchMode, ColorMatchMethod, ColorProbe};
> ```

- [ ] **Step 4: 改调用点 — run_color_watcher 签名与内部调用**

修改 `watcher.rs:229` 的 `run_color_watcher` 函数签名，在 `match_mode` 参数后加 `match_method`：

```rust
async fn run_color_watcher(
    app: AppHandle,
    card_id: String,
    probes: Vec<crate::audio::types::ColorProbe>,
    match_mode: crate::audio::types::ColorMatchMode,
    match_method: crate::audio::types::ColorMatchMethod,
    playback_tx: std::sync::mpsc::Sender<player::AudioCommand>,
    cooldown_ms: u32,
    poll_interval_ms: u32,
    audio_enabled: bool,
    cancel: Arc<AtomicBool>,
) {
```

修改 `watcher.rs:285` 的调用，补第 4 参数：

```rust
        let result = match_color_probes(&screenshots, &probes, match_mode.clone(), match_method.clone());
```

- [ ] **Step 5: 改 spawn 点 — 透传 method**

修改 `watcher.rs:103-119` 处的 `ColorWatch` 分支。将：
```rust
            super::types::AudioTriggerMode::ColorWatch => {
                let probes = card.color_probes.clone();
                let match_mode = card.color_match_mode.clone();
                tauri::async_runtime::spawn(async move {
                    run_color_watcher(
                        app_clone,
                        card_id,
                        probes,
                        match_mode,
                        playback_tx_clone,
                        cooldown_ms,
                        poll_interval_ms,
                        audio_enabled,
                        cancel_clone,
                    )
                        .await;
                });
            }
```
改为（新增 `match_method` 快照与透传）：
```rust
            super::types::AudioTriggerMode::ColorWatch => {
                let probes = card.color_probes.clone();
                let match_mode = card.color_match_mode.clone();
                let match_method = card.color_match_method.clone();
                tauri::async_runtime::spawn(async move {
                    run_color_watcher(
                        app_clone,
                        card_id,
                        probes,
                        match_mode,
                        match_method,
                        playback_tx_clone,
                        cooldown_ms,
                        poll_interval_ms,
                        audio_enabled,
                        cancel_clone,
                    )
                        .await;
                });
            }
```

- [ ] **Step 6: 运行测试确认通过**

Run: `cargo test --manifest-path src-tauri/Cargo.toml match_color_probes`
Expected: PASS（含既有 `match_color_probes_all_mode_all_hit` 等 4 个旧用例需补第 4 参数——见 Step 7）

- [ ] **Step 7: 修复既有 match_color_probes 测试调用**

既有 4 个旧测试（`watcher.rs:1079/1094/1109/1116` 附近）调用 `match_color_probes` 缺第 4 参数，会编译失败。在每个调用补 `ColorMatchMethod::Average` 作为第 4 参数（保持原 average 语义不变）：

```rust
        let result = match_color_probes(&screenshots, &probes, ColorMatchMode::All, ColorMatchMethod::Average);
```
对 4 处调用逐一补 `ColorMatchMethod::Average`。

Run: `cargo test --manifest-path src-tauri/Cargo.toml match_color_probes`
Expected: PASS（全部用例）

- [ ] **Step 8: 全量编译检查**

Run: `cargo check --manifest-path src-tauri/Cargo.toml`
Expected: 编译通过（mod.rs 的 `audio_test_color_match` 仍用旧 average 判定，不依赖新签名——Task 4 改造）

- [ ] **Step 9: 提交**

```bash
git add src-tauri/src/audio/watcher.rs
git commit -m "feat(audio): match_color_probes 接 method 参数并透传至 watcher（Issue #63）"
```

---

## Task 4: Rust 测试命令 — audio_test_color_match 复用 probe_hit

**Files:**
- Modify: `src-tauri/src/audio/mod.rs:46-58`（ColorProbeTestResult）、`:468-528`（命令）
- Test: 无新增单测（沿用 Task 2/3 覆盖；命令路径靠手动验证）

**Interfaces:**
- Consumes: Task 2 的 `probe_hit`、Task 1 的 `ColorMatchMethod`
- Produces: `ColorProbeTestResult` 增 `matching_pixel_count` 字段；`audio_test_color_match` 按 method 判定

- [ ] **Step 1: 扩展 ColorProbeTestResult 字段**

修改 `mod.rs:46-58` 的 `ColorProbeTestResult`，在 `tolerance` 字段后追加：

```rust
#[derive(Debug, Clone, serde::Serialize)]
#[serde(rename_all = "camelCase")]
pub struct ColorProbeTestResult {
    /// 该 probe 是否命中
    pub matched: bool,
    /// 截取区域平均 RGB
    pub sampled_color: [u8; 3],
    /// 与目标颜色的距离
    pub distance: f32,
    /// 目标颜色
    pub target_color: [u8; 3],
    /// 容差
    pub tolerance: u8,
    /// anyPixel 命中像素数；average 恒 0
    pub matching_pixel_count: usize,
}
```

- [ ] **Step 2: 改 audio_test_color_match — 读取 method 并复用 probe_hit**

将 `mod.rs:468-528` 的 `audio_test_color_match` 整体替换为：

```rust
#[tauri::command]
pub async fn audio_test_color_match(
    _app: tauri::AppHandle,
    state: tauri::State<'_, AudioState>,
    card_id: String,
) -> Result<ColorTestResult, AppError> {
    let (probes, match_mode, match_method) = {
        let inner = state.lock_inner().map_err(|e| AppError::from(e))?;
        let card = inner
            .settings
            .cards
            .iter()
            .find(|c| c.id == card_id)
            .ok_or_else(|| AppError::from("卡片不存在".to_string()))?;

        if card.trigger_mode != types::AudioTriggerMode::ColorWatch {
            return Err(AppError::from("只有识色模式卡片支持识色测试".to_string()));
        }
        if card.color_probes.is_empty() {
            return Err(AppError::from("未配置识色探针".to_string()));
        }
        (card.color_probes.clone(), card.color_match_mode.clone(), card.color_match_method.clone())
    };

    let mut probe_results: Vec<ColorProbeTestResult> = Vec::with_capacity(probes.len());
    let mut hit_count = 0usize;

    for probe in &probes {
        let region = probe.region.as_ref().ok_or_else(|| {
            AppError::from("存在未框选区域的探针，请先框选再测试".to_string())
        })?;
        let captured = match watcher::capture_region(region) {
            Some(img) => img,
            None => return Err(AppError::from("截图失败".to_string())),
        };
        // 复用统一判定：count_only=false → anyPixel 全扫拿命中数与最近像素
        let hit = watcher::probe_hit(&captured, probe, match_method.clone(), false);
        if hit.matched {
            hit_count += 1;
        }
        probe_results.push(ColorProbeTestResult {
            matched: hit.matched,
            sampled_color: hit.sampled_color,
            distance: hit.distance,
            target_color: probe.target_color,
            tolerance: probe.tolerance,
            matching_pixel_count: hit.matching_pixel_count,
        });
    }

    let triggered = match match_mode {
        types::ColorMatchMode::All => hit_count == probes.len(),
        types::ColorMatchMode::Any => hit_count > 0,
    };

    Ok(ColorTestResult {
        triggered,
        hit_count,
        total_count: probes.len(),
        probes: probe_results,
    })
}
```

- [ ] **Step 3: 全量编译检查**

Run: `cargo check --manifest-path src-tauri/Cargo.toml`
Expected: 编译通过

- [ ] **Step 4: 全量 Rust 测试**

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS（既有 types.rs 用例中如有手构 `AudioCard` 字面量需补 `color_match_method`——见 Step 5）

- [ ] **Step 5: 修复 types.rs 手构 AudioCard 字面量**

`types.rs:252-291` 的 `audio_card_with_color_watch_roundtrip` 测试手构 `AudioCard { ... }`，新增字段后需补 `color_match_method`。在该字面量中（`color_match_mode: ColorMatchMode::Any,` 之后）补：

```rust
            color_match_method: ColorMatchMethod::AnyPixel,
```

并在该测试断言中可选追加：
```rust
    assert_eq!(back.color_match_method, ColorMatchMethod::AnyPixel);
```

> 注：若 `types.rs` 测试模块 `use super::*` 已覆盖 `ColorMatchMethod`（Task 1 添加的枚举在 `super`），无需额外 import。

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add src-tauri/src/audio/mod.rs src-tauri/src/audio/types.rs
git commit -m "feat(audio): audio_test_color_match 复用 probe_hit 并返回命中像素数（Issue #63）"
```

---

## Task 5: 前端类型 — ColorMatchMethod

**Files:**
- Modify: `src/components/app/audio-types.ts`
- Test: `src/components/app/audio-utils.test.ts`（Task 6 覆盖）

**Interfaces:**
- Consumes: 无
- Produces: `type ColorMatchMethod = "average" | "anyPixel"`；`AudioCard`/`AudioCardForm` 增 `colorMatchMethod`；`DEFAULT_AUDIO_CARD.colorMatchMethod = "average"`

- [ ] **Step 1: 新增类型与字段**

在 `src/components/app/audio-types.ts` 的 `ColorMatchMode` 类型定义之后（line 3 附近）追加：

```ts
export type ColorMatchMethod = "average" | "anyPixel";
```

在 `AudioCard` 类型（line 25-47）的 `colorMatchMode` 字段后追加：

```ts
    colorMatchMode: ColorMatchMode;
    colorMatchMethod: ColorMatchMethod;
```

在 `AudioCardForm` 类型（line 64-85）的 `colorMatchMode` 字段后追加：

```ts
    colorMatchMode: ColorMatchMode;
    colorMatchMethod: ColorMatchMethod;
```

在 `DEFAULT_AUDIO_CARD`（line 87-106）的 `colorMatchMode: "all",` 后追加：

```ts
    colorMatchMode: "all",
    colorMatchMethod: "average",
```

- [ ] **Step 2: 类型检查**

Run: `bun run build`（含 tsc）
Expected: `audio-utils.ts` 因 `cardToForm`/`parseCardForm` 未处理新字段可能报缺省回退类型错误——见 Task 6 修复。若仅 tsc 报 form 转换缺字段，先在 Task 6 修复后再编译。

> 注：若 `settingsToForm`/`parseSettingsForm` 因新增必填字段报类型错误，属预期，Task 6 修复。

- [ ] **Step 3: 提交（与 Task 6 合并提交，此步可暂不提交）**

跳过单独提交，Task 6 完成后一并提交。

---

## Task 6: 前端 utils — cardToForm / parseCardForm 透传

**Files:**
- Modify: `src/components/app/audio-utils.ts:53-77`（cardToForm）、`:167-187`（parseCardForm 返回）
- Test: `src/components/app/audio-utils.test.ts`

**Interfaces:**
- Consumes: Task 5 的 `ColorMatchMethod` 类型
- Produces: `cardToForm`/`parseCardForm` 透传 `colorMatchMethod`

- [ ] **Step 1: 写失败测试 — colorMatchMethod roundtrip 与缺省**

在 `src/components/app/audio-utils.test.ts` 的 `describe("audio-utils", ...)` 内追加新 describe：

```ts
    describe("colorMatchMethod", () => {
        it("cardToForm 透传 colorMatchMethod", () => {
            const settings = {
                audioEnabled: true,
                cards: [{
                    ...DEFAULT_AUDIO_CARD,
                    id: "c1",
                    name: "识色卡",
                    triggerMode: "colorWatch",
                    colorMatchMethod: "anyPixel",
                    colorProbes: [],
                    audioFiles: ["a.mp3"],
                }],
            };
            const form = settingsToForm(settings);
            expect(form.cards[0].colorMatchMethod).toBe("anyPixel");
        });

        it("cardToForm 缺省 colorMatchMethod 回退 average", () => {
            const settings = {
                audioEnabled: true,
                cards: [{
                    ...DEFAULT_AUDIO_CARD,
                    id: "c1",
                    name: "识色卡",
                    triggerMode: "colorWatch",
                    audioFiles: ["a.mp3"],
                }],
            };
            const form = settingsToForm(settings);
            expect(form.cards[0].colorMatchMethod).toBe("average");
        });

        it("parseSettingsForm roundtrip 透传 colorMatchMethod", () => {
            const form = {
                audioEnabled: true,
                cards: [{
                    ...DEFAULT_AUDIO_CARD,
                    id: "c1",
                    name: "识色卡",
                    triggerMode: "colorWatch",
                    colorProbes: [{
                        region: {x: 0, y: 0, width: 2, height: 2},
                        targetColor: "#ff0000",
                        tolerance: "10",
                    }],
                    colorMatchMode: "all",
                    colorMatchMethod: "anyPixel",
                    audioFiles: ["a.mp3"],
                    volume: "0.8",
                    cooldownMs: "1000",
                    watchMatchThreshold: "0.75",
                    watchPollIntervalMs: "500",
                    comboWindowMs: "60000",
                    comboWindows: [],
                    hotkey: "",
                    watchRegion: null,
                    watchReferenceImagePath: "",
                    playMode: "single",
                    allowSimultaneous: false,
                    enabled: true,
                }],
            };
            const settings = parseSettingsForm(form);
            expect(settings.cards[0].colorMatchMethod).toBe("anyPixel");
        });
    });
```

> 注：`DEFAULT_AUDIO_CARD` 含 `colorMatchMethod: "average"`，故第二用例（不显式设）期望 `"average"`。第三用例 `form` 显式传 `"anyPixel"`。

- [ ] **Step 2: 运行测试确认失败**

Run: `bunx vitest run src/components/app/audio-utils.test.ts`
Expected: FAIL — `cardToForm` 未输出 `colorMatchMethod`（undefined !== "anyPixel"）

- [ ] **Step 3: 改 cardToForm — 补 colorMatchMethod**

在 `audio-utils.ts:53-77` 的 `cardToForm` 返回对象中，`colorMatchMode: card.colorMatchMode ?? "all",` 之后追加：

```ts
        colorMatchMode: card.colorMatchMode ?? "all",
        colorMatchMethod: card.colorMatchMethod ?? "average",
```

- [ ] **Step 4: 改 parseCardForm — 读取并回写**

在 `audio-utils.ts:124` 附近（`const colorMatchMode = form.colorMatchMode ?? "all";` 之后）追加：

```ts
    const colorMatchMode = form.colorMatchMode ?? "all";
    const colorMatchMethod = form.colorMatchMethod ?? "average";
```

在 `parseCardForm` 返回对象（line 167-186）的 `colorMatchMode,` 之后追加：

```ts
        colorMatchMode,
        colorMatchMethod,
```

- [ ] **Step 5: 运行测试确认通过**

Run: `bunx vitest run src/components/app/audio-utils.test.ts`
Expected: PASS

- [ ] **Step 6: 全量前端测试与构建**

Run: `bun run test && bun run build`
Expected: 全部测试 PASS，tsc + vite build 成功

- [ ] **Step 7: 提交（含 Task 5）**

```bash
git add src/components/app/audio-types.ts src/components/app/audio-utils.ts src/components/app/audio-utils.test.ts
git commit -m "feat(audio): 前端新增 colorMatchMethod 类型与透传（Issue #63）"
```

---

## Task 7: 前端 UI — 匹配方式下拉与聚合模式改名

**Files:**
- Modify: `src/components/app/audio-page.tsx:734-752`（匹配模式区块）、`:233-237`（测试 toast）

**Interfaces:**
- Consumes: Task 5 的 `ColorMatchMethod` 类型
- Produces: 「匹配方式」下拉（average/anyPixel）；原「匹配模式」改名「聚合模式」；测试 toast 追加命中像素数

- [ ] **Step 1: 新增「匹配方式」下拉 + 改名「聚合模式」**

将 `audio-page.tsx:734-752` 处的「匹配模式」Field 区块：

```tsx
                {isColor && (
                    <FieldGroup>
                        <Field>
                            <FieldLabel>匹配模式</FieldLabel>
                            <FieldContent>
                                <Select
                                    value={card.colorMatchMode}
                                    onValueChange={(v) => onUpdate({colorMatchMode: v as "all" | "any"})}
                                >
                                    <SelectTrigger>
                                        <SelectValue/>
                                    </SelectTrigger>
                                    <SelectContent>
                                        <SelectItem value="all">全部命中才触发</SelectItem>
                                        <SelectItem value="any">任一命中即触发</SelectItem>
                                    </SelectContent>
                                </Select>
                            </FieldContent>
                        </Field>
```

替换为（前置新增「匹配方式」Field，原 Field 改名「聚合模式」）：

```tsx
                {isColor && (
                    <FieldGroup>
                        <Field>
                            <FieldLabel>匹配方式</FieldLabel>
                            <FieldContent>
                                <Select
                                    value={card.colorMatchMethod}
                                    onValueChange={(v) => onUpdate({colorMatchMethod: v as "average" | "anyPixel"})}
                                >
                                    <SelectTrigger>
                                        <SelectValue/>
                                    </SelectTrigger>
                                    <SelectContent>
                                        <SelectItem value="average">识色1 · 区域平均色</SelectItem>
                                        <SelectItem value="anyPixel">识色2 · 单像素命中</SelectItem>
                                    </SelectContent>
                                </Select>
                            </FieldContent>
                        </Field>
                        <Field>
                            <FieldLabel>聚合模式</FieldLabel>
                            <FieldContent>
                                <Select
                                    value={card.colorMatchMode}
                                    onValueChange={(v) => onUpdate({colorMatchMode: v as "all" | "any"})}
                                >
                                    <SelectTrigger>
                                        <SelectValue/>
                                    </SelectTrigger>
                                    <SelectContent>
                                        <SelectItem value="all">全部命中才触发</SelectItem>
                                        <SelectItem value="any">任一命中即触发</SelectItem>
                                    </SelectContent>
                                </Select>
                            </FieldContent>
                        </Field>
```

- [ ] **Step 2: 适配测试 toast — 追加命中像素数**

在 `audio-page.tsx` 顶部的 `handleTestColorMatch`（line 214-244）内，找到内联类型 `ColorProbeResult`（line 217-223），补可选字段：

```ts
            type ColorProbeResult = {
                matched: boolean;
                sampledColor: [number, number, number];
                distance: number;
                targetColor: [number, number, number];
                tolerance: number;
                matchingPixelCount?: number;
            };
```

将 `detail` 计算（line 233-235）：

```ts
                const detail = result.probes
                    .map((p, i) => `#${i + 1}: ${p.matched ? "命中" : "未中"} (采样 #${p.sampledColor.map((v) => v.toString(16).padStart(2, "0")).join("")} 距离 ${p.distance.toFixed(1)})`)
                    .join("\n");
```

替换为：

```ts
                const detail = result.probes
                    .map((p, i) => {
                        const sample = p.sampledColor.map((v) => v.toString(16).padStart(2, "0")).join("");
                        const count = p.matchingPixelCount && p.matchingPixelCount > 0 ? ` 命中${p.matchingPixelCount}px` : "";
                        return `#${i + 1}: ${p.matched ? "命中" : "未中"} (采样 #${sample} 距离 ${p.distance.toFixed(1)}${count})`;
                    })
                    .join("\n");
```

- [ ] **Step 3: 构建验证**

Run: `bun run build`
Expected: tsc + vite build 成功

- [ ] **Step 4: 提交**

```bash
git add src/components/app/audio-page.tsx
git commit -m "feat(audio): UI 新增匹配方式下拉并改名聚合模式（Issue #63）"
```

---

## Task 8: 手动验证与收尾

**Files:**
- 无代码改动；运行验证

- [ ] **Step 1: 启动桌面开发**

Run: `bun run tauri dev`
Expected: 应用启动，进入音频模块识色卡片

- [ ] **Step 2: 验证 average 模式（识色1）回归**

- 新增识色卡，框选小区域，设目标色 + 容差
- 「匹配方式」选「识色1 · 区域平均色」
- 点「实时识色测试」→ toast 显示 `#1: 命中 (采样 #xxxx 距离 y.y)`，无"命中 Npx"后缀
- Expected: average 模式行为与改造前一致

- [ ] **Step 3: 验证 anyPixel 模式（识色2）**

- 「匹配方式」切「识色2 · 单像素命中」
- 框选区域内放一个目标色像素（或对准含目标色的画面）
- 点「实时识色测试」→ toast 显示 `#1: 命中 (采样 #xxxx 距离 0.0 命中Npx)`
- Expected: anyPixel 模式命中时显示命中像素数

- [ ] **Step 4: 验证持久化**

- 设置 method=anyPixel，等 autosave 落盘，重启应用
- Expected: 重启后 method 仍为 anyPixel

- [ ] **Step 5: 验证正交聚合**

- 两探针 + 匹配方式 anyPixel + 聚合模式「全部命中才触发」
- 两探针都命中 → 触发；仅一命中 → 不触发
- Expected: 跨探针聚合与单探针匹配方式正交工作

- [ ] **Step 6: 全量测试回归**

Run: `cargo test --manifest-path src-tauri/Cargo.toml && bun run test`
Expected: 全部 PASS

- [ ] **Step 7: 文档收尾**

检查 `CLAUDE.md` 的音频模块描述是否需补充「识色2」概念。若「音频模块」段未具体描述识色探针实现，无需改动（CLAUDE.md 只列模块职责，不列实现细节）。

Expected: 通常无需改动 CLAUDE.md。

- [ ] **Step 8: 最终提交（如有收尾改动）**

若 Step 7 有改动：
```bash
git add CLAUDE.md
git commit -m "docs: 音频模块补充识色2说明（Issue #63）"
```

若无改动，跳过。

---

## 完成标准

- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` 全 PASS
- [ ] `bun run test` 全 PASS
- [ ] `bun run build` 成功（tsc + vite）
- [ ] 手动验证：average/anyPixel 两模式行为正确，持久化正常，聚合正交
- [ ] 7 个 commit（Task 1-7 各一，Task 8 视情况）
