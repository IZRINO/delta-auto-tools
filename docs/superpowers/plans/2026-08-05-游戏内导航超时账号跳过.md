# 游戏内导航超时账号跳过 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 多账号自动轮次的游戏内导航步骤超时时，标记当前账号“状态不确定”并继续下一个账号；导航执行系统错误仍全面暂停。

**Architecture:** `game_navigation` 将“步骤超时”从泛化的 `Paused` 结果中拆出为 `TimedOut`。`special_ops::mod` 通过纯映射函数把 `TimedOut` 变为账号级 `AccountRunError`，保持 `Paused`、紧急停止及后端资源故障为系统级错误。既有 `round_runner` 已按账号级错误保存状态并继续后续账号，不修改其生产逻辑。

**Tech Stack:** Rust、Tokio、Tauri 2、现有 `special_ops` 状态机与 Rust 单元测试。

---

## 文件职责

- 修改：`src-tauri/src/special_ops/game_navigation.rs`
  - 只负责产生精确的导航结果类型；步骤时间耗尽必须返回 `TimedOut`，driver 返回错误必须继续返回 `Paused`。
- 修改：`src-tauri/src/special_ops/mod.rs`
  - 只负责把导航结果映射到 round 的账号级或系统级错误，并保留独立导航试运行的全局暂停行为。
- 修改：`src-tauri/src/special_ops/round_runner.rs`
  - 只增加导航账号级错误继续下一账号的回归测试；不改 loop 行为。
- 修改：`droid-wiki/features/special-ops.md`
  - 记录独立试运行与多账号 round 的不同导航超时语义。
- 修改：`docs/superpowers/specs/2026-07-30-特勤处多账号制作自动轮次-design.md`
  - 修正轮次状态机和故障分类。

不创建 Tauri command、不改前端、不改持久化 schema。当前工作树脏且未获提交授权，本计划不执行 `git commit`。

### Task 1: 拆分导航超时结果

**Files:**
- Modify: `src-tauri/src/special_ops/game_navigation.rs:55-63`
- Modify: `src-tauri/src/special_ops/game_navigation.rs:218-242`
- Test: `src-tauri/src/special_ops/game_navigation.rs:431-620`

- [x] **Step 1: 写失败测试，证明超时与执行错误必须不同类**

在 `game_navigation.rs` 的 `tests` module 增加以下测试。第一个 future 永不完成，第二个 future 立即返回 driver 错误：

```rust
#[tokio::test(start_paused = true)]
async fn navigation_timeout_is_distinct_from_execution_failure() {
    let cancelled = Arc::new(AtomicBool::new(false));
    let mut on_step = |_| {};
    let timeout = run_navigation_step(
        GameNavigationStep::WaitModeReady,
        &cancelled,
        &mut on_step,
        std::future::pending::<Result<(), String>>(),
    );
    tokio::pin!(timeout);
    tokio::task::yield_now().await;
    tokio::time::advance(STEP_TIMEOUT).await;
    assert_eq!(
        timeout.await,
        Err(GameNavigationResult::TimedOut {
            failed_step: GameNavigationStep::WaitModeReady,
        })
    );

    let execution_error = run_navigation_step(
        GameNavigationStep::OpenSpecialOps,
        &cancelled,
        &mut on_step,
        async { Err("游戏窗口恢复失败".to_string()) },
    )
    .await;
    assert_eq!(
        execution_error,
        Err(GameNavigationResult::Paused {
            failed_step: GameNavigationStep::OpenSpecialOps,
            message: "游戏窗口恢复失败".to_string(),
        })
    );
}
```

- [x] **Step 2: 运行测试，确认现有实现不满足新结果类型**

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml navigation_timeout_is_distinct_from_execution_failure
```

Expected: 编译失败，`GameNavigationResult` 不存在 `TimedOut`。

- [x] **Step 3: 最小实现结果类型与超时分支**

在 enum 中加入：

```rust
TimedOut {
    failed_step: GameNavigationStep,
},
```

将 `run_navigation_step` 的 timer 分支改为：

```rust
_ = tokio::time::sleep(STEP_TIMEOUT) => Err(GameNavigationResult::TimedOut {
    failed_step: step,
}),
```

不要修改 `result = future` 分支；它继续保留 `Paused { failed_step, message }`，以便窗口、截图和输入错误仍具系统级语义。

- [x] **Step 4: 运行定向测试，确认状态机行为**

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml game_navigation::tests
```

Expected: `navigation_timeout_is_distinct_from_execution_failure` 与现有导航顺序、取消测试全部通过。

### Task 2: 在 round 中将仅超时降为账号级错误

**Files:**
- Modify: `src-tauri/src/special_ops/mod.rs:4991-5010`
- Modify: `src-tauri/src/special_ops/mod.rs:5142-5177`
- Test: `src-tauri/src/special_ops/mod.rs:12357-12426`

- [x] **Step 1: 写失败测试，锁定 round 映射与账号状态**

在 `mod.rs` 测试 module 增加：

```rust
#[test]
fn round_navigation_timeout_is_account_scoped_without_station() {
    let error = map_round_navigation_result(game_navigation::GameNavigationResult::TimedOut {
        failed_step: game_navigation::GameNavigationStep::WaitStationGrid,
    })
    .unwrap_err();
    assert_eq!(error.scope, round_runner::ErrorScope::Account);
    assert_eq!(error.station, None);
    assert_eq!(error.step, "navigation.WaitStationGrid");
    assert_eq!(error.message, "步骤超时");

    let mut settings = LoginFixture::complete().settings;
    let before = settings.accounts[0].stations.clone();
    apply_round_account_failure(&mut settings, "selected", &error, 1_000).unwrap();
    assert_eq!(settings.accounts[0].status, AccountStatus::Uncertain);
    assert_eq!(settings.accounts[0].stations, before);
}

#[test]
fn round_navigation_execution_failure_remains_system_scoped() {
    let error = map_round_navigation_result(game_navigation::GameNavigationResult::Paused {
        failed_step: game_navigation::GameNavigationStep::OpenSpecialOps,
        message: "游戏窗口恢复失败".to_string(),
    })
    .unwrap_err();
    assert_eq!(error.scope, round_runner::ErrorScope::System);
    assert_eq!(error.step, "navigation.OpenSpecialOps");
}
```

在测试 module 顶部按现有 import 风格导入 `GameNavigationResult` 与 `GameNavigationStep`；不要使用字符串匹配推断错误类型。

- [x] **Step 2: 运行测试，确认映射函数尚不存在**

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml round_navigation_timeout_is_account_scoped_without_station
```

Expected: 编译失败，`map_round_navigation_result` 不存在。

- [x] **Step 3: 加入纯映射函数并接入 ProductionRoundDriver**

在 `map_round_ammo_stop` 附近添加：

```rust
fn map_round_navigation_result(
    result: game_navigation::GameNavigationResult,
) -> Result<(), round_runner::AccountRunError> {
    match result {
        game_navigation::GameNavigationResult::Ready => Ok(()),
        game_navigation::GameNavigationResult::TimedOut { failed_step } => Err(
            round_runner::AccountRunError::account(
                format!("navigation.{failed_step:?}"),
                "步骤超时",
            ),
        ),
        game_navigation::GameNavigationResult::Paused {
            failed_step,
            message,
        } => Err(round_runner::AccountRunError::system(
            format!("navigation.{failed_step:?}"),
            message,
        )),
        game_navigation::GameNavigationResult::EmergencyStopped => Err(
            round_runner::AccountRunError::system("round.stopped", "多账号轮次已停止"),
        ),
    }
}
```

将 `ProductionRoundDriver::navigate` 末尾原有 `match result` 替换为：

```rust
map_round_navigation_result(result)
```

`apply_round_account_failure` 无需修改：无 `station` 的账号级 `navigation.*` 错误已经会将账号设为 `Uncertain`，且不会触碰四制作台。

- [x] **Step 4: 让独立导航试运行穷尽新结果类型**

在 `run_navigation_worker` 的两个 match 中把 `TimedOut { failed_step }` 视为导航失败：

```rust
game_navigation::GameNavigationResult::TimedOut { failed_step } => (
    LoginRunStatus::Failed,
    format!("游戏内导航超时（{failed_step:?}）"),
),
```

保存分支使用：

```rust
matches!(
    result,
    game_navigation::GameNavigationResult::TimedOut { .. }
        | game_navigation::GameNavigationResult::Paused { .. }
)
```

这样独立导航试运行仍持久化全局暂停。

- [x] **Step 5: 运行定向映射与状态持久化测试**

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml round_navigation_
```

Expected: 两个新增映射测试通过；账号状态为 `Uncertain`，执行错误保持系统级。

### Task 3: 回归验证账号跳过与系统暂停

**Files:**
- Modify: `src-tauri/src/special_ops/round_runner.rs:311-371`
- Test: `src-tauri/src/special_ops/round_runner.rs:311-371`

- [x] **Step 1: 增加导航超时账号级错误回归测试**

在 `round_runner.rs` tests 中增加：

```rust
#[tokio::test]
async fn navigation_timeout_persists_current_account_then_runs_next_account() {
    let driver = FakeDriver::new(
        vec![
            Err(AccountRunError::account(
                "navigation.WaitStationGrid",
                "步骤超时",
            )),
            Ok(AccountRunSuccess {
                processed_stations: 1,
            }),
        ],
        false,
    );

    let result = run_round(&driver, &plan(), Arc::new(AtomicBool::new(false))).await;

    assert_eq!(result.stop, RoundStop::Completed);
    assert_eq!(
        driver.actions(),
        ["run:a", "persist-account:a", "run:b", "close-game"]
    );
}
```

- [x] **Step 2: 运行回归测试**

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml navigation_timeout_persists_current_account_then_runs_next_account
cargo test --manifest-path src-tauri/Cargo.toml system_failure_pauses_and_never_runs_next_account
```

Expected: 导航超时继续账号 `b`；系统错误只运行账号 `a` 并持久化暂停。

### Task 4: 同步行为文档与执行完整验证

**Files:**
- Modify: `droid-wiki/features/special-ops.md:43-46`
- Modify: `droid-wiki/features/special-ops.md:131-133`
- Modify: `docs/superpowers/specs/2026-07-30-特勤处多账号制作自动轮次-design.md:87-97`
- Modify: `docs/superpowers/specs/2026-07-30-特勤处多账号制作自动轮次-design.md:178-195`

- [x] **Step 1: 更新 wiki**

在“游戏内导航试运行”段明确：独立试运行的导航超时与执行错误均全局暂停。多账号 round 段明确：只有 `GameNavigationResult::TimedOut` 触发账号 `Uncertain` 并切换下一账号；`Paused` 的窗口、截图、输入等执行错误保持系统级全面暂停。

- [x] **Step 2: 更新原多账号设计文档**

将轮次状态机中的 `GameNavigation` 从无条件系统失败改为两条分支：

```text
GameNavigation TimedOut
  -> PersistAccountFailure
  -> NextAccount

GameNavigation Paused / SystemFailure
  -> PersistPause
  -> Paused
```

在“账号级故障”加入导航步骤超时；在“系统级故障”加入导航窗口、截图、输入与 runtime 错误。

- [x] **Step 3: 检查格式与索引**

Run:

```powershell
git diff --check
codegraph sync
```

Expected: `git diff --check` 无输出；CodeGraph 索引同步成功。若本机 `codegraph` 命令仍不在 PATH，记录该阻塞，不得宣称索引已同步。

- [x] **Step 4: 运行 Rust 验证**

Run:

```powershell
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo check --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml
```

Expected: 全部通过；Rust 全量测试无新增失败。

- [x] **Step 5: 运行项目质量门禁**

Run:

```powershell
bun run check
```

Expected: TypeScript、Vitest、coverage、Rust fmt、Clippy 与 Rust tests 全部通过。若既有脏工作树导致非本次失败，完整报告失败命令与相关输出，不回滚用户改动。
