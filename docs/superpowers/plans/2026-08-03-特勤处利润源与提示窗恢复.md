# 特勤处利润源与提示窗恢复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans for inline execution. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 KKRB 繁忙可继续配置、Moligod 文件锁不破坏查询结果、操作提示窗慢加载不再自动暂停轮次。

**Architecture:** scheduler 增加“可重试动作结果”，只将操作提示窗加载超时映射为一秒后的重新调度。Moligod 在成功取得网页结果后将数据目录清理降级为后台 best-effort。前端只改错误文案与操作提示窗的首次状态同步。

**Tech Stack:** Rust、Tokio、Tauri 2、React 19、TypeScript、Vitest。

---

### Task 1: scheduler 可重试启动结果

**Files:**
- Modify: `src-tauri/src/special_ops/round_scheduler.rs`
- Modify: `src-tauri/src/special_ops/mod.rs`
- Test: `src-tauri/src/special_ops/round_scheduler.rs`

- [x] **Step 1: 写失败测试**

为 fake driver 加入一次 `RetryAfter(Duration::from_secs(1))` 结果；断言第一次执行后 `pause_automation` 未调用，等待一秒后同一动作再次执行。

- [x] **Step 2: 运行失败测试**

Run: `cargo test --manifest-path src-tauri/Cargo.toml round_scheduler::tests::retryable_action_keeps_scheduler_armed --lib`

Expected: FAIL，`SchedulerActionOutcome` 尚不存在或 scheduler 将动作错误视为暂停。

- [x] **Step 3: 最小实现**

将 `SchedulerDriver::execute_action` 的成功值改为 `SchedulerActionOutcome`。加入 `Completed` 与 `RetryAfter(Duration)` 两种结果；后者通过已有 `wait_for_wake` 等待，以便暂停通知可立即打断。`SpecialOpsSchedulerDriver` 仅将固定的操作提示窗加载超时映射为 `RetryAfter(1 秒)`；其他错误保留失败语义。

- [x] **Step 4: 运行通过测试**

Run: `cargo test --manifest-path src-tauri/Cargo.toml round_scheduler::tests --lib`

Expected: PASS。

### Task 2: Moligod 文件锁降级

**Files:**
- Modify: `src-tauri/src/special_ops/profit/moligod.rs`
- Test: `src-tauri/src/special_ops/profit/moligod.rs`

- [x] **Step 1: 写失败测试**

抽取完成查询的纯函数。传入成功网页结果、成功销毁结果和失败清理结果，断言返回网页结果；传入销毁失败，断言返回销毁错误。

- [x] **Step 2: 运行失败测试**

Run: `cargo test --manifest-path src-tauri/Cargo.toml special_ops::profit::moligod::tests::cleanup_failure_does_not_replace_query_result --lib`

Expected: FAIL，当前逻辑把清理错误返回给调用方。

- [x] **Step 3: 最小实现**

成功构建隐藏 WebView 后，把数据目录清理放入 Tokio 后台任务；任务在有限次数内重试并最终仅写 warning。查询返回路径只检查窗口销毁和网页查询结果。

- [x] **Step 4: 运行通过测试**

Run: `cargo test --manifest-path src-tauri/Cargo.toml special_ops::profit::moligod::tests --lib`

Expected: PASS。

### Task 3: 前端恢复提示

**Files:**
- Modify: `src/components/app/special-ops-profit-filter.tsx`
- Modify: `src/components/app/special-ops-profit-filter.test.tsx`
- Modify: `src/components/app/special-ops-operation-overlay.tsx`
- Modify: `src/components/app/special-ops-operation-overlay.test.ts`

- [x] **Step 1: 写失败测试**

断言 KKRB `-101` 错误展示“可直接填写精确名称”；断言操作提示窗挂载后调用 bootstrap 并采用其 `runSnapshot`。

- [x] **Step 2: 运行失败测试**

Run: `bunx vitest run src/components/app/special-ops-profit-filter.test.tsx src/components/app/special-ops-operation-overlay.test.ts`

Expected: FAIL，当前提示无手工填写说明，提示窗只依赖事件。

- [x] **Step 3: 最小实现**

只在 KKRB 繁忙错误上补充手工填写说明。操作提示窗挂载时调用现有 `special_ops_get_bootstrap`，若存在 `runSnapshot` 则写入本地 state；保留现有事件订阅更新。

- [x] **Step 4: 运行通过测试**

Run: `bunx vitest run src/components/app/special-ops-profit-filter.test.tsx src/components/app/special-ops-operation-overlay.test.ts`

Expected: PASS。

### Task 4: 文档与全量验证

**Files:**
- Modify: `droid-wiki/features/special-ops.md`
- Modify: `docs/superpowers/specs/2026-08-03-特勤处利润源与提示窗恢复-design.md`

- [x] **Step 1: 更新功能文档**

记录 KKRB 手工填写路径、Moligod 清理降级规则和提示窗超时的一秒重试规则。

- [x] **Step 2: 运行验证**

Run: `bun run test`

Expected: PASS。

Run: `bun run build`

Expected: PASS。

Run: `cargo test --manifest-path src-tauri/Cargo.toml --lib`

Expected: PASS；若开发版仍占用 exe，记录全量 test 受文件锁阻断并至少完成 lib 测试。

Run: `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`

Expected: PASS。

Run: `git diff --check`

Expected: PASS。

用户要求暂不提交，省略 commit 步骤。
