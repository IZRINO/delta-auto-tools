# 三角洲行动工具上下文

三角洲行动工具是面向《三角洲行动》玩家的桌面辅助工具集合，包含多个可独立使用的工具能力。

## Language

本文档中 **Delta** 是《三角洲行动》的英文简写，二者等价，不另作术语定义。涉及代码中的 `delta_` 前缀（如 `delta_list_accounts`、
`delta-accounts` 路由 ID）均为工程命名，语义指向三角洲行动相关功能。

**账号管理**:
管理游戏账号登录、令牌刷新和删除的独立页面，支持 QQ / 微信 / QQSafe / Wegame QQ / Wegame 微信 / 先遣服 6
种鉴权流程。页面负责账号全生命周期：添加（扫码登录）→ 令牌状态监控 → 手动/自动刷新 → 重新登录 → 删除。
_Avoid_: 账号中心、登录页、用户管理

**游戏数据**:
通过游戏 API 查询角色信息的独立页面，包含无参数仪表盘（7 个 API 自动加载）和查询工作台（6 个参数化 API 按需调用）。依赖 QQ
或微信账号的 `GameAuth` 鉴权。
_Avoid_: 数据面板、游戏面板、数据中心

**工具箱**:
Wegame 运营操作、QQSafe 封禁查询与先遣服测试列表的独立页面。三种功能按选中账号类型动态渲染，互不重叠。
_Avoid_: 工具页、运营页、辅助页

**查询工作台**:
游戏数据页内的参数化查询区域，通过下拉选择 API 类型、动态渲染参数表单并展示结果。当前支持物品查询、物价查询、枪械详情、操作日志、改装方案、地图推荐
6 种查询。
_Avoid_: 查询面板、搜索区、API浏览器

**令牌状态**:
账号访问令牌的有效性判定，分四级：有效（绿点，距过期 >3 天）、即将过期（黄点 + 剩余天数，距过期 ≤3
天但未过期）、已过期（红点，expiresAt ≤ 当前时间）、无过期信息（灰色点，expiresAt 为 null 或 accessToken 为 null）。Wegame 和部分
QQSafe 账号可能无过期时间字段。
_Avoid_: token状态、凭证状态、认证状态

**账号能力**:
根据账号类型决定可用的功能域。QQ 和微信账号可查游戏数据（acctype 分别为 qc 和 wx）；Wegame QQ 和 Wegame
微信账号可领取保险箱礼包和每日抽卡；QQSafe 账号可查封禁记录和游戏报告；先遣服账号可查测试游戏列表。能力互不重叠。
_Avoid_: 账号权限、账号功能、账号角色

**登录 Dialog**:
模态对话框引导用户完成扫码登录流程：选择账号类型 → 显示二维码并自动轮询 → 登录成功后获取访问令牌。三种模式：QQ
扫码模式（QQ/QQSafe/WegameQQ/Pioneer 共享结构，返回 qrImage+qrToken+qrSig+loginSig+cookie）、微信扫码模式（WeChat/WegameWeChat，返回
qrCode URL+uuid）、Wegame 令牌模式（WegameQQ 返回 tgpId+tgpTicket，WegameWeChat 返回 tgpId+tgpTicket）。关闭 Dialog
时立即停止轮询定时器。
_Avoid_: 登录页、登录向导、登录面板

**自动刷新**:
调用 API 失败时检测令牌过期，自动调用对应类型的 `update_access_token` 命令刷新令牌，成功后更新全局 Context
并重试原请求。刷新失败（cookie 也过期或类型不支持刷新如 Wegame/QQSafe）则标记账号为"需重新登录"。同一账号的并发刷新请求通过
Promise 去重，避免重复调用。
_Avoid_: 静默刷新、后台刷新、隐形刷新

**分批加载**:
游戏数据页选中账号后先并行加载 player 和 record 两个核心摘要 API，渲染 PageHero 指标和两个核心卡片后，再并行加载
assets、recent、achievement、password、bind 五个详情 API。切换账号时通过版本号机制（loadVersion）取消过期请求的回调处理。
_Avoid_: 懒加载、渐进加载、流式加载

**版本号机制**:
游戏数据页用于取消过期请求的方案：每次切换账号递增 loadVersion 计数器，API 请求的回调中检查当前 loadVersion
是否等于发起时的值，不等则丢弃响应。因 Tauri invoke 不支持 AbortController，此为替代方案。
_Avoid_: 请求取消、请求版本

**Pioneer 账号标识**:
先遣服账号的身份判定问题。Pioneer 登录走 QQ 流程但产出 key（而非 openid+accessToken），Rust 后端 AccountKind 枚举当前无
Pioneer 变体。前端需通过 extraJson 中的标记或扩展 AccountKind 来区分。此为待决项。
_Avoid_: 先遣服标记、Pioneer标记

**GameAuth**:
游戏数据 API 的鉴权结构，包含 openid、accessToken 和 acctype 三个字段。acctype 由账号类型决定：QQ 系为 "qc"，微信系为 "wx"
。Wegame 账号不使用此结构。
_Avoid_: 游戏鉴权、游戏凭证

**WegameTicket**:
Wegame 运营操作（开箱/抽卡）的鉴权结构，包含 id（Wegame UIN）和 ticket（tgpTicket）两个字段。Wegame 账号的 accessToken 字段实际存储
tgpTicket 值。
_Avoid_: Wegame鉴权、Wegame凭证

**QQ 扫码模式**:
QQ / QQSafe / WegameQQ / Pioneer 共享的登录流程模式。第一步调用 `get_login_qr` 获取二维码（base64 图片 + qrToken + qrSig +
loginSig + cookie），第二步以 2 秒间隔轮询 `poll_login_status`（返回状态码 0=成功 1=等待 2=已扫描 -2=过期 -3=拒绝
-4=错误），第三步调用 `get_access_token` 获取访问令牌。二维码有效期约 30 秒。
_Avoid_: QR登录模式、二维码模式

**微信扫码模式**:
WeChat / WegameWeChat 共享的登录流程模式。第一步调用 `get_login_qr` 获取微信扫码 URL（qrCode）和 uuid，第二步轮询
`poll_status`（返回状态码 1=等待 2=已扫描 3=成功 -2=超时 -3=拒绝 -4=错误），第三步用 wxCode 调用 `get_access_token`
获取令牌。微信二维码有效期约 5 分钟。
_Avoid_: 微信模式、WeChat模式

**摩斯密码解析**:
通过屏幕采样区域识别摩斯密码并输出数字结果的工具。
_Avoid_: Morse、识别器

**计时器**:
通过用户自定义快捷键启动，并在透明窗口中显示倒计时状态的独立工具。
_Avoid_: 计算器、倒计时器

**连发器**:
通过用户按住触发键持续触发目标键盘输入，并在松开时自动补齐触发次数为偶数的独立工具。
_Auto_: 键盘连发、连发功能、按键连发
_Avoid_: 连点器、自动按键

**连发器卡片**:
代表一个可单独配置触发键、目标键和连发间隔的连发器条目。
_Avoid_: 规则、配置项

**同步工具**:
计时器、计数器、连发器三类共享「透明窗口同步显示运行态 + 位置设置 + 快捷键分组重启 + 总开关停止运行态」生命周期的工具统称。摩斯密码解析不属此类（使用区域框选 overlay 与单键严格 scope，无透明窗口与持续运行态会话）。
_Avoid_: 同步模块、sync工具（实现名）

**触发键**:
用户按住以启动连发的按键。
_Avoid_: 热键、快捷键

**目标键**:
连发过程中被持续触发的按键。
_Avoid_: 输出键、执行键

**连发间隔**:
两次目标键触发之间的时间间隔，单位毫秒。
_Avoid_: 周期、频率

**补齐触发**:
当松开触发键时累计触发次数为奇数，由本次连发会话额外触发一次使总数变为偶数后再结束的机制。
_Avoid_: 补发、修正

**连发会话**:
一次触发键 Down 到对应 worker 线程完成补齐并退出的独立运行实例。
_Avoid_: 全局任务、共享计时器

**计时器卡片**:
代表一个可单独命名、配置时长和绑定快捷键的计时器条目。
_Avoid_: 任务、规则、配置项

**计时器总开关**:
控制所有计时器卡片与透明窗口是否可用的功能级开关。
_Avoid_: 文本框开关、显示开关

**可停止**:
工具运行态会话可被全局总开关停止的属性。同步工具（计时器/计数器/连发器）可停止；摩斯密码解析无运行态会话，不可停止。此为工具实现的契约，而非在全局开关处硬编码列举。
_Avoid_: 可中断、stoppable

**透明窗口**:
覆盖在屏幕上的无边框置顶显示面板，用于在不遮挡游戏操作的情况下呈现工具状态。
_Avoid_: overlay、悬浮层

**位置设置**:
用户通过可视化框选确定透明窗口显示位置的配置流程。
_Avoid_: 坐标配置、拖拽设置

**透明窗口显示宽度**:
用户可调整的透明窗口横向尺寸，用于让计时器名称和倒计时文本在游戏内保持可读。
_Avoid_: 文本框宽度、卡片宽度

**计时器卡片排序**:
用户通过拖动计时器卡片改变其展示顺序的配置能力。
_Avoid_: 优先级、分组、队列

## Relationships

- 一个**三角洲行动工具**包含多个独立工具；**账号管理**、**游戏数据**和**工具箱**是其中三个并列的功能页面，与摩斯密码解析、计时器、计数器、连发器同级。
- **计时器**、**计数器**、**连发器**统称**同步工具**，共享透明窗口同步显示、位置设置、快捷键分组重启与总开关停止运行态的生命周期；**摩斯密码解析**不属同步工具。
- **可停止**是同步工具实现的契约；全局总开关关闭时遍历可停止工具停止其运行态会话，而非在全局开关处硬编码列举。
- 一个**账号管理**页管理所有类型的游戏账号，不区分账号类型。
- **账号选中**是应用级全局状态，**账号管理**、**游戏数据**和**工具箱**三个页面共享同一选中账号 ID。
- **账号能力**决定数据页和工具箱页渲染哪些功能区域，不可用区域不渲染（非灰显）。
- **登录 Dialog** 用于在**账号管理**页添加新账号，是模态流程不离开当前页。关闭 Dialog 时立即停止轮询定时器。
- **令牌状态**决定账号是否可用于 API 调用，过期时触发**自动刷新**。
- **自动刷新**仅对支持 `update_access_token` 的账号类型生效（QQ / 微信 / 先遣服）；Wegame 和 QQSafe 不支持自动刷新，过期后只能重新扫码登录。
- **自动刷新**失败时账号标记为"需重新登录"，需在**账号管理**页重新扫码。同一账号的并发刷新请求通过 Promise 去重。
- **游戏数据**页采用**分批加载**策略，首批加载 player+record 核心摘要，次批加载其余 5 个详情 API。
- **游戏数据**页切换账号时通过**版本号机制**丢弃过期请求响应。
- **查询工作台**处理需要额外参数的游戏 API 调用，与无参数仪表盘卡片视觉区分明确。
- **工具箱**页根据选中账号类型动态展示 Wegame 操作、QQSafe 封禁查询或先遣服测试列表，三种功能互斥不重叠。
- QQ 和微信账号构造 **GameAuth** 用于游戏数据查询，acctype 分别为 "qc" 和 "wx"。
- Wegame 账号构造 **WegameTicket** 用于运营操作，id 为 UIN、ticket 为 tgpTicket。
- QQSafe 账号的封禁查询需要 openid、accessToken 和 JWT 解码后的 code 三个字段。
- **QQ 扫码模式**涵盖 QQ、QQSafe、WegameQQ、Pioneer 四种账号类型，共享二维码获取和轮询逻辑。
- **微信扫码模式**涵盖 WeChat 和 WegameWeChat 两种账号类型，共享微信二维码获取和轮询逻辑。
- Pioneer 账号的**Pioneer 账号标识**问题待决：Rust 后端 AccountKind 枚举当前无 Pioneer 变体。
- 游戏 API 返回 `ApiResponse<Value>`（非结构化 JSON），前端需动态渲染或逐步补充类型定义。
- 一个**计时器**包含多个**计时器卡片**。
- **计时器总开关**关闭时，所有**计时器卡片**不可触发，计时器**透明窗口**不显示。
- **计时器**配置需要保留在本地，重启应用后仍可恢复。
- 多个**计时器卡片**共享一个计时器**透明窗口**。
- 一个计时器**透明窗口**通过一次计时器**位置设置**确定屏幕位置。
- 计时器**透明窗口显示宽度**属于计时器**透明窗口**的本地配置。
- **计时器卡片排序**同时决定设置页中的卡片顺序与计时器**透明窗口**中的文本顺序。
- 一个**计数器**包含多个**计数器卡片**；计数器是与计时器完全独立的工具，拥有独立的页面、设置文件和 Tauri 命令。
- **计数器总开关**关闭时，所有**计数器卡片**不可触发，计数器**透明窗口**不显示。
- **计数器**配置需要保留在本地，重启应用后仍可恢复。
- 多个**计数器卡片**共享一个计数器**透明窗口**。
- 一个计数器**透明窗口**通过一次计数器**位置设置**确定屏幕位置。
- 计数器**透明窗口显示宽度**属于计数器**透明窗口**的本地配置。
- **计数器卡片排序**同时决定设置页中的卡片顺序与计数器**透明窗口**中的文本顺序。
- 计数器运行态保存在 `counter_state.json`（`src-tauri/src/counter/counter_state.rs`），与 `counter_settings.json` 平行，独立持久化。
- **连发器**的**触发键**不能与**摩斯密码解析**、**计时器**、**计数器**或其他**连发器卡片**的热键冲突。
- 每次按下**触发键**都会为匹配的**连发器卡片**创建独立**连发会话**，同一卡片快速重触不会取消旧会话的补齐收尾。
- 当**组合触发键**与同主键**单键触发键**并存时，按下组合键会同时创建组合键和单键的**连发会话**；松开修饰键只结束组合键会话，单键会话持续到主键抬起。
- **目标键**触发必须表现为真实按下与抬起，按下到抬起的持续时间在 8-12ms 内抖动。

## Example dialogue

> **Dev:** "计时器和摩斯密码解析共用同一个工作流吗？"
> **Domain expert:** "不，计时器是当前工具菜单下的独立工具，只参考摩斯的位置设置交互。"

> **Dev:** "Wegame 账号的 accessToken 存的是什么？"
> **Domain expert:** "Wegame 的 access_token 字段实际存的是 tgpTicket，用于构造 WegameTicket { id, ticket }
> 进行开箱和抽卡操作。"

> **Dev:** "Pioneer 账号在 AccountKind 里怎么区分？"
> **Domain expert:** "这是个待决项。当前 Rust 后端只有 5 种 AccountKind（qq/wechat/qqsafe/wegame_qq/wegame_wechat），没有
> Pioneer 变体。Pioneer 走 QQ 登录流程但产出 key 而非 openid+accessToken，需要在前端通过 extraJson
> 标记或扩展后端枚举来区分。"

> **Dev:** "游戏 API 返回的结构是确定的吗？"
> **Domain expert:** "Rust 后端返回 ApiResponse<Value>，即非结构化 JSON。前端 Phase 1
> 用动态渲染，后续根据实际数据补充类型定义。"

> **Dev:** "QQSafe 的 code 参数从哪来？"
> **Domain expert:** "QQSafe 登录成功后 JWT 存储在 account.extraJson 中，前端需 base64 解码 JWT payload 提取 code 字段。如果
> extraJson 为 null，则无法查询封禁记录，需提示重新登录。"

## Flagged ambiguities

- "计算器名称"被使用过一次，已解析为**计时器**名称。
- **Pioneer 账号标识**方案待决：扩展 AccountKind 枚举 vs extraJson 标记 vs 前端判定。
- 游戏 API 返回值结构化时机待决：Phase 1 用 Value + 动态渲染 vs 定义 typed DTO。
- 令牌过期检测机制待决：后端是否使用特定 code 标识过期 vs 前端 msg 文本匹配 vs expiresAt 预检。
