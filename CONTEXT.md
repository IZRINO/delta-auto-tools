# 三角洲行动工具上下文

三角洲行动工具是面向《三角洲行动》玩家的桌面辅助工具集合，包含多个可独立使用的工具能力。

## Language

**摩斯密码解析**:
通过屏幕采样区域识别摩斯密码并输出数字结果的工具。
_Avoid_: Morse、识别器

**计时器**:
通过用户自定义快捷键启动，并在透明窗口中显示倒计时状态的独立工具。
_Avoid_: 计算器、倒计时器

**连发器**:
通过用户按住触发键持续触发目标键盘输入，并在松开时自动补齐触发次数为偶数的独立工具。
_Auto_: 键盘连发、连发功能、按键连发
_Avoid_: 连点器、自动按键

**连发器卡片**:
代表一个可单独配置触发键、目标键和连发间隔的连发器条目。
_Avoid_: 规则、配置项

**触发键**:
用户按住以启动连发的按键。
_Avoid_: 热键、快捷键

**目标键**:
连发过程中被持续触发的按键。
_Avoid_: 输出键、执行键

**连发间隔**:
两次目标键触发之间的时间间隔，单位毫秒。
_Avoid_: 周期、频率

**补齐触发**:
当松开触发键时累计触发次数为奇数，在下一个间隔后自动额外触发一次使总数变为偶数的机制。
_Avoid_: 补发、修正

**计时器卡片**:
代表一个可单独命名、配置时长和绑定快捷键的计时器条目。
_Avoid_: 任务、规则、配置项

**计时器总开关**:
控制所有计时器卡片与透明窗口是否可用的功能级开关。
_Avoid_: 文本框开关、显示开关

**透明窗口**:
覆盖在屏幕上的无边框置顶显示面板，用于在不遮挡游戏操作的情况下呈现工具状态。
_Avoid_: overlay、悬浮层

**位置设置**:
用户通过可视化框选确定透明窗口显示位置的配置流程。
_Avoid_: 坐标配置、拖拽设置

**透明窗口显示宽度**:
用户可调整的透明窗口横向尺寸，用于让计时器名称和倒计时文本在游戏内保持可读。
_Avoid_: 文本框宽度、卡片宽度

**计时器卡片排序**:
用户通过拖动计时器卡片改变其展示顺序的配置能力。
_Avoid_: 优先级、分组、队列

## Relationships

- 一个**三角洲行动工具**包含一个或多个独立工具。
- **摩斯密码解析**、**计时器**和**连发器**是并列的独立工具。
- 一个**计时器**包含多个**计时器卡片**。
- **计时器总开关**关闭时，所有**计时器卡片**不可触发，**透明窗口**不显示。
- 关闭主窗口表示退出整个**三角洲行动工具**，需要同时清理后台运行状态和所有**透明窗口**。
- **计时器**配置需要保留在本地，重启应用后仍可恢复。
- 多个**计时器卡片**共享一个**透明窗口**。
- 一个**透明窗口**通过一次**位置设置**确定屏幕位置。
- **透明窗口显示宽度**属于**透明窗口**的本地配置。
- **计时器卡片排序**同时决定设置页中的卡片顺序与**透明窗口**中的文本顺序。
- 一个**连发器**包含多个**连发器卡片**。
- **连发器总开关**关闭时，所有**连发器卡片**不可触发，**连发器透明窗口**不显示。
- 多个**连发器卡片**共享一个**连发器透明窗口**。
- **连发器透明窗口**通过一次**位置设置**确定屏幕位置。
- **连发器卡片排序**同时决定设置页中的卡片顺序与**连发器透明窗口**中的文本顺序。
- **连发器配置**需要保留在本地，重启应用后仍可恢复。
- **连发器**的**触发键**不能与**摩斯密码解析**、**计时器**或其他**连发器卡片**的热键冲突。

## Example dialogue

> **Dev:** “计时器和摩斯密码解析共用同一个工作流吗？”
> **Domain expert:** “不，计时器是当前工具菜单下的独立工具，只参考摩斯的位置设置交互。”

## Flagged ambiguities

- “计算器名称”被使用过一次，已解析为**计时器**名称。
