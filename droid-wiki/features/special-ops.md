# 特勤处自动化（开发中）

`special_ops` 保存账号级制作台与子弹兑换配置，按账号顺序生成调度快照。每个账号包含 4 台制作台；到期制作台在同一账号内聚合处理。每日兑换时间固定按 `Asia/Shanghai` 的 `HH:mm` 解释，已成功目标按自然日跳过。

当前 vertical slice 提供：Rust 配置持久化、账号/制作台/子弹状态模型、暂停状态、兑换唤醒时间、兑换前 5 分钟合并窗口、React 工作台展示与 Tauri 状态事件。WeGame 登录、OCR、校准、键鼠执行和崩溃恢复尚未实现，不能视为自动化完成。
